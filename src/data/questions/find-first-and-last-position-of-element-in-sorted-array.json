{
  "id": "UPIDtcp4IukiahtZbO0K",
  "slug": "find-first-and-last-position-of-element-in-sorted-array",
  "rank": 25,
  "title": "Find First and Last Position of Element in Sorted Array",
  "difficulty": "medium",
  "topics": [
    "Binary Search"
  ],
  "externalUrl": "https://www.leetcode.com/problems/find-first-and-last-position-of-element-in-sorted-array",
  "numViews": 820,
  "questionBody": "<p>Given an array of integers <code>nums</code> sorted in non-decreasing order, find the starting and ending position of a given <code>target</code> value.</p>\n\n<p>If <code>target</code> is not found in the array, return <code>[-1, -1]</code>.</p>\n\n<p>You must&nbsp;write an algorithm with&nbsp;<code>O(log n)</code> runtime complexity.</p>\n\n<p>&nbsp;</p>\n<p><strong class=\"example\">Example 1:</strong></p>\n<pre><strong>Input:</strong> nums = [5,7,7,8,8,10], target = 8\n<strong>Output:</strong> [3,4]\n</pre><p><strong class=\"example\">Example 2:</strong></p>\n<pre><strong>Input:</strong> nums = [5,7,7,8,8,10], target = 6\n<strong>Output:</strong> [-1,-1]\n</pre><p><strong class=\"example\">Example 3:</strong></p>\n<pre><strong>Input:</strong> nums = [], target = 0\n<strong>Output:</strong> [-1,-1]\n</pre>\n<p>&nbsp;</p>\n<p><strong>Constraints:</strong></p>\n\n<ul>\n\t<li><code>0 &lt;= nums.length &lt;= 10<sup>5</sup></code></li>\n\t<li><code>-10<sup>9</sup>&nbsp;&lt;= nums[i]&nbsp;&lt;= 10<sup>9</sup></code></li>\n\t<li><code>nums</code> is a non-decreasing array.</li>\n\t<li><code>-10<sup>9</sup>&nbsp;&lt;= target&nbsp;&lt;= 10<sup>9</sup></code></li>\n</ul>\n",
  "clarifyingQuestions": [
    "What is the range of integer values that can be present in the `nums` array?",
    "Can the input array `nums` be empty or null?",
    "If the target is not found, should I return `[-1, -1]` specifically, or is there another specified return value for this case?",
    "If the target appears multiple times, are we guaranteed that the array is sorted in non-decreasing order?",
    "Can I assume that the input array `nums` is already sorted in ascending order?"
  ],
  "edgeCases": [
    {
      "edgeCase": "Empty array",
      "handling": "Return [-1, -1] if the input array is empty as the target cannot exist."
    },
    {
      "edgeCase": "Target is smaller than the smallest element in the array",
      "handling": "Binary search will not find the target and return [-1, -1]."
    },
    {
      "edgeCase": "Target is larger than the largest element in the array",
      "handling": "Binary search will not find the target and return [-1, -1]."
    },
    {
      "edgeCase": "Array with one element and target matches the element",
      "handling": "Return [0, 0] if the single element matches the target."
    },
    {
      "edgeCase": "Array with one element and target does not match the element",
      "handling": "Return [-1, -1] if the single element does not match the target."
    },
    {
      "edgeCase": "Array with all elements equal to the target",
      "handling": "Binary search to find the leftmost and rightmost indices will correctly identify the start and end positions."
    },
    {
      "edgeCase": "Target exists only once in the array",
      "handling": "Binary search for leftmost and rightmost will return the same index for both, resulting in [index, index]."
    },
    {
      "edgeCase": "Large array sizes to consider potential time complexity bottlenecks",
      "handling": "Binary search ensures a logarithmic time complexity, handling large array sizes efficiently."
    }
  ],
  "companies": [
    {
      "slug": "amazon",
      "name": "Amazon",
      "logo": "/logos/amazon.jpg"
    },
    {
      "slug": "meta",
      "name": "Meta",
      "logo": "/logos/meta.jpg"
    },
    {
      "slug": "microsoft",
      "name": "Microsoft",
      "logo": "/logos/microsoft.jpg"
    },
    {
      "slug": "bloomberg-lp",
      "name": "Bloomberg",
      "logo": "/logos/bloomberg-lp.jpg"
    },
    {
      "slug": "google",
      "name": "Google",
      "logo": "/logos/google.jpg"
    },
    {
      "slug": "tiktok",
      "name": "TikTok",
      "logo": "/logos/tiktok.png"
    },
    {
      "slug": "apple",
      "name": "Apple",
      "logo": "/logos/apple.jpg"
    },
    {
      "slug": "uber",
      "name": "Uber",
      "logo": "/logos/uber.jpg"
    },
    {
      "slug": "yahoo",
      "name": "Yahoo",
      "logo": "/logos/yahoo.jpg"
    },
    {
      "slug": "linkedin",
      "name": "LinkedIn",
      "logo": "/logos/linkedin.png"
    },
    {
      "slug": "adobe",
      "name": "Adobe",
      "logo": "/logos/adobe.jpg"
    },
    {
      "slug": "instacart",
      "name": "Instacart",
      "logo": "/logos/instacart.jpg"
    },
    {
      "slug": "netapp",
      "name": "NetApp",
      "logo": "/logos/netapp.jpg"
    },
    {
      "slug": "splunk",
      "name": "Splunk",
      "logo": "/logos/splunk.jpg"
    },
    {
      "slug": "pinterest",
      "name": "Pinterest",
      "logo": "/logos/pinterest.png"
    },
    {
      "slug": "paypal",
      "name": "PayPal",
      "logo": "/logos/paypal.jpg"
    },
    {
      "slug": "salesforce",
      "name": "Salesforce",
      "logo": "/logos/salesforce.png"
    },
    {
      "slug": "servicenow",
      "name": "ServiceNow",
      "logo": "/logos/servicenow.jpg"
    },
    {
      "slug": "capgemini",
      "name": "Capgemini",
      "logo": "/logos/capgemini.jpg"
    },
    {
      "slug": "zillow",
      "name": "Zillow",
      "logo": "/logos/zillow.jpg"
    },
    {
      "slug": "de-shaw",
      "name": "De Shaw",
      "logo": "/logos/de-shaw.jpg"
    },
    {
      "slug": "goldman-sachs",
      "name": "Goldman Sachs",
      "logo": "/logos/goldman-sachs.jpg"
    },
    {
      "slug": "atlassian",
      "name": "Atlassian",
      "logo": "/logos/atlassian.png"
    }
  ],
  "solutions": {
    "bruteForce": {
      "overview": "The brute-force method to find the first and last spot of a number in a sorted list is to simply look at each number in the list, one by one. We check each number to see if it is equal to the target number we are searching for.",
      "steps": [
        "Start by looking at the very first number in the list.",
        "Check if this number is the target number we are searching for.",
        "If it is, remember this spot as a possible first and last spot and keep going.",
        "Move to the next number in the list.",
        "Again, check if this number is the target number.",
        "If it is, and we haven't found any target numbers yet, mark this as both the first and last spot.",
        "If we've already found the target number, update the last spot to be the current spot.",
        "Continue this process of checking each number one by one until you reach the end of the list.",
        "Once you reach the end of the list, if you never found the target number, report that it's not in the list. If you found the number at least once, report the first and last spots you found."
      ],
      "bigO": {
        "runtimeValue": "O(n)",
        "runtimeExplanation": "The algorithm iterates through the input array of size n exactly once. In each iteration, a constant-time comparison is performed to check if the current element equals the target value. The number of operations is directly proportional to the size of the input array. Therefore, the time complexity is O(n).",
        "spaceValue": "O(1)",
        "spaceExplanation": "The provided brute-force algorithm iterates through the input array and maintains two variables, one for the first index and one for the last index where the target element is found. These variables store integer values representing array indices. Since the space used for these variables does not depend on the size of the input array (N), the auxiliary space is constant."
      },
      "languages": [
        "Python",
        "JavaScript",
        "TypeScript",
        "Java",
        "C++",
        "C#",
        "Go",
        "Rust",
        "Kotlin",
        "Swift",
        "Ruby",
        "PHP"
      ],
      "code": {
        "Python": "def find_first_and_last_brute_force(sorted_numbers, target_number):\n    first_position = -1\n    last_position = -1\n\n    for index in range(len(sorted_numbers)):\n        # Check each number to see if it equals the target\n        if sorted_numbers[index] == target_number:\n\n            # If we haven't found the target yet, mark the first position\n            if first_position == -1:\n                first_position = index\n                last_position = index\n\n            # Otherwise, update the last position\n            else:\n                last_position = index\n\n    return [first_position, last_position]\n",
        "JavaScript": "function findFirstAndLastPositionBruteForce(numbers, target) {\n  let firstPosition = -1;\n  let lastPosition = -1;\n\n  for (let index = 0; index < numbers.length; index++) {\n    if (numbers[index] === target) {\n      // If we find the target number.\n\n      if (firstPosition === -1) {\n        // This is the first time finding target.\n        firstPosition = index;\n\n      }\n\n      lastPosition = index;\n      // Update the last position\n\n    }\n  }\n\n  return [firstPosition, lastPosition];\n}\n",
        "TypeScript": "function findFirstAndLastPositionBruteForce(sortedNumbers: number[], targetNumber: number): number[] {\n  let firstPosition: number = -1;\n  let lastPosition: number = -1;\n\n  for (let index = 0; index < sortedNumbers.length; index++) {\n    const currentNumber: number = sortedNumbers[index];\n\n    if (currentNumber === targetNumber) {\n      // If we found the target, and haven't marked a first position\n      if (firstPosition === -1) {\n        firstPosition = index;\n\n      }\n\n      // Always update the last position if we find the target\n      lastPosition = index;\n    }\n  }\n\n  // If we didn't find the target number, report -1 for both positions\n  if (firstPosition === -1) {\n    return [-1, -1];\n  }\n\n  return [firstPosition, lastPosition];\n}",
        "Java": "public class Solution {\n    public int[] searchRange(int[] nums, int target) {\n        int firstPosition = -1;\n        int lastPosition = -1;\n\n        // Iterate through the array to find target\n        for (int index = 0; index < nums.length; index++) {\n            if (nums[index] == target) {\n\n                //If firstPosition is -1, this is the first time target is found\n                if (firstPosition == -1) {\n                    firstPosition = index;\n\n                }\n                \n                //Always update the last position to the current index.\n                lastPosition = index;\n            }\n        }\n\n        int[] result = {firstPosition, lastPosition};\n\n        return result;\n    }\n}\n",
        "C++": "#include <vector>\n\nstd::vector<int> findFirstAndLastPositionBruteForce(std::vector<int>& numbers, int target) {\n    int firstPosition = -1;\n    int lastPosition = -1;\n\n    for (int index = 0; index < numbers.size(); ++index) {\n        if (numbers[index] == target) {\n            // If target is found, record its position.\n\n            if (firstPosition == -1) {\n                firstPosition = index;\n            }\n\n            // Always update the last position to the current index\n\n            lastPosition = index;\n        }\n    }\n\n    // Construct the result vector containing first and last positions\n\n    return {firstPosition, lastPosition};\n}",
        "C#": "public class Solution {\n    public int[] SearchRange(int[] numbers, int target) {\n        int firstPosition = -1;\n        int lastPosition = -1;\n\n        // Iterate through the array to find the target.\n        for (int index = 0; index < numbers.Length; index++)\n        {\n            if (numbers[index] == target)\n            {\n                // If firstPosition is -1, it means target is found for the first time.\n                if (firstPosition == -1)\n                {\n                    firstPosition = index;\n\n                }\n\n                // Continuously update lastPosition to find the last occurence.\n                lastPosition = index;\n            }\n        }\n\n        // Return the result array with first and last positions.\n        return new int[] { firstPosition, lastPosition };\n    }\n}",
        "Go": "package main\n\nfunc searchRangeBruteForce(numbers []int, target int) []int {\n\tfirstPosition := -1\n\tlastPosition := -1\n\n\tfor index := 0; index < len(numbers); index++ {\n\t\tnumber := numbers[index]\n\n\t\t// Check if current number is the target\n\t\tif number == target {\n\t\t\tif firstPosition == -1 {\n\t\t\t\t//Set first and last to index if target found first time.\n\t\t\t\tfirstPosition = index\n\t\t\t}\n\n\t\t\t//Always update the last position if target found.\n\t\t\tlastPosition = index\n\n\t\t}\n\t}\n\n\treturn []int{firstPosition, lastPosition}\n}\n",
        "Rust": "fn find_first_and_last_brute_force(numbers: Vec<i32>, target: i32) -> Vec<i32> {\n    let mut first_position: i32 = -1;\n    let mut last_position: i32 = -1;\n\n    for index in 0..numbers.len() {\n        // Check if the current number matches target\n        if numbers[index] == target {\n            // If this is the first occurence, record it\n            if first_position == -1 {\n                first_position = index as i32;\n            }\n\n            // Update the last position for every match\n            last_position = index as i32;\n        }\n    }\n\n    //Construct and return the result array\n    if first_position != -1 {\n        return vec![first_position, last_position];\n    } else {\n        // Indicate target not found\n        return vec![-1, -1];\n    }\n}",
        "Kotlin": "fun findFirstAndLastPositionBruteForce(\n    numbers: IntArray,\n    target: Int\n): IntArray {\n    var firstPosition: Int = -1\n    var lastPosition: Int = -1\n\n    for (index in numbers.indices) {\n        if (numbers[index] == target) {\n            // If this is the first time seeing the target\n            if (firstPosition == -1) {\n                firstPosition = index\n                \n            }\n            \n            // Always update the last position when target found\n            lastPosition = index\n        }\n    }\n\n    // Target not found case\n    if (firstPosition == -1) {\n        return intArrayOf(-1, -1)\n    }\n\n    return intArrayOf(firstPosition, lastPosition)\n}\n",
        "Swift": "func findFirstAndLastPositionBruteForce(\n    numbers: [Int],\n    target: Int\n) -> [Int] {\n    var firstPosition: Int = -1\n    var lastPosition: Int = -1\n\n    for index in 0..<numbers.count {\n        let currentNumber = numbers[index]\n\n        if currentNumber == target {\n            // If we haven't found it yet, mark both\n            // the first and last indices\n            if firstPosition == -1 {\n\n                firstPosition = index\n\n            }\n\n            // Update the last position as we go\n            lastPosition = index\n        }\n    }\n\n    if firstPosition != -1 {\n        return [firstPosition, lastPosition]\n    } else {\n        // Indicate target not present\n        return [-1, -1]\n    }\n}\n",
        "Ruby": "def find_first_and_last_position_brute_force(sorted_array, target):\n    first_position = -1\n    last_position = -1\n\n    for index in range(len(sorted_array)):\n        current_number = sorted_array[index]\n\n        if current_number == target:\n            # If we haven't found the number yet, mark it as both first and last\n            if first_position == -1:\n                first_position = index\n\n            # Update the last position to the current index\n            last_position = index\n\n    result = [first_position, last_position]\n    return result",
        "PHP": "<?php\nfunction findFirstAndLastPositionBruteForce(array $numbers, int $target):\narray\n{\n    $firstPosition = -1;\n    $lastPosition = -1;\n\n    $numbersLength = count($numbers);\n\n    for ($index = 0; $index < $numbersLength; $index++) {\n        if ($numbers[$index] === $target) {\n            // We found a target, need to record it\n\n            if ($firstPosition === -1) {\n                // If this is the first time we've found the target, record it.\n\n                $firstPosition = $index;\n                \n            }\n\n            // Update the last position to be the current index\n            $lastPosition = $index;\n\n        }\n    }\n\n    return [$firstPosition, $lastPosition];\n}\n?>"
      }
    },
    "optimal": {
      "overview": "Because the numbers are sorted, we can use a very efficient search strategy. Instead of checking each number individually, we repeatedly divide the search area in half to quickly narrow down the location of the number we are looking for and where it starts and ends.",
      "steps": [
        "First, use the divide-in-half search to find the very first occurrence of the number.",
        "To do this, if the middle number is too small, only look at the right half. If the middle number is bigger, only look at the left half. If the middle number is our target, check if the number before it is also our target. If it is, keep searching on the left half.",
        "Next, do the same divide-in-half search to find the very last occurrence of the number.",
        "If the middle number is too small, only look at the right half. If the middle number is bigger, only look at the left half. If the middle number is our target, check if the number after it is also our target. If it is, keep searching on the right half.",
        "Once you've found both the first and last positions, you have your answer."
      ],
      "bigO": {
        "runtimeValue": "O(log n)",
        "runtimeExplanation": "The algorithm employs binary search to find both the first and last occurrences of the target value. Binary search repeatedly halves the search interval. Finding the first occurrence takes O(log n) time, and finding the last occurrence also takes O(log n) time. Since we perform two independent binary searches, the overall time complexity is O(log n) + O(log n), which simplifies to O(log n).",
        "spaceValue": "O(1)",
        "spaceExplanation": "The algorithm described uses a binary search approach, repeatedly dividing the search space in half. It only requires a few constant space variables to store the start, end, and middle indices for the search. The space used does not depend on the input size N (the number of elements in the sorted array). Therefore, the auxiliary space complexity is O(1)."
      },
      "languages": [
        "Python",
        "JavaScript",
        "TypeScript",
        "Java",
        "C++",
        "C#",
        "Go",
        "Rust",
        "Kotlin",
        "Swift",
        "Ruby",
        "PHP"
      ],
      "code": {
        "Python": "def find_first_and_last_position(nums, target):\n    def find_first(nums, target):\n        left_index = 0\n        right_index = len(nums) - 1\n        first_position = -1\n\n        while left_index <= right_index:\n            middle_index = (left_index + right_index) // 2\n\n            if nums[middle_index] < target:\n                left_index = middle_index + 1\n            elif nums[middle_index] > target:\n                right_index = middle_index - 1\n            else:\n                # Potential first occurence found.\n                first_position = middle_index\n\n                # Search left side for the first.\n                right_index = middle_index - 1\n\n        return first_position\n\n    def find_last(nums, target):\n        left_index = 0\n        right_index = len(nums) - 1\n        last_position = -1\n\n        while left_index <= right_index:\n            middle_index = (left_index + right_index) // 2\n\n            if nums[middle_index] < target:\n                left_index = middle_index + 1\n            elif nums[middle_index] > target:\n                right_index = middle_index - 1\n            else:\n                # Potential last occurence found.\n                last_position = middle_index\n\n                # Search right side for the last.\n                left_index = middle_index + 1\n\n        return last_position\n\n    first_position = find_first(nums, target)\n    last_position = find_last(nums, target)\n\n    return [first_position, last_position]\n",
        "JavaScript": "function searchRange(numbers, target) {\n  const notFound = [-1, -1];\n  if (!numbers || numbers.length === 0) {\n    return notFound;\n  }\n\n  const firstOccurrence = findFirst(numbers, target);\n  const lastOccurrence = findLast(numbers, target);\n\n  return [firstOccurrence, lastOccurrence];\n}\n\nfunction findFirst(numbers, target) {\n  let leftIndex = 0;\n  let rightIndex = numbers.length - 1;\n  let firstPosition = -1;\n\n  while (leftIndex <= rightIndex) {\n    let middleIndex = Math.floor((leftIndex + rightIndex) / 2);\n\n    if (numbers[middleIndex] < target) {\n      // Target is in the right half\n      leftIndex = middleIndex + 1;\n    } else if (numbers[middleIndex] > target) {\n      rightIndex = middleIndex - 1;\n    } else {\n      // Found a match, check if it's the first\n      firstPosition = middleIndex;\n\n      // Keep searching left for the first occurrence\n      rightIndex = middleIndex - 1;\n    }\n  }\n  return firstPosition;\n}\n\nfunction findLast(numbers, target) {\n  let leftIndex = 0;\n  let rightIndex = numbers.length - 1;\n  let lastPosition = -1;\n\n  while (leftIndex <= rightIndex) {\n    let middleIndex = Math.floor((leftIndex + rightIndex) / 2);\n\n    if (numbers[middleIndex] < target) {\n      // Target is in the right half\n      leftIndex = middleIndex + 1;\n    } else if (numbers[middleIndex] > target) {\n      rightIndex = middleIndex - 1;\n    } else {\n      // Found a match, check if it's the last\n      lastPosition = middleIndex;\n\n      // Keep searching right for the last occurrence\n      leftIndex = middleIndex + 1;\n    }\n  }\n  return lastPosition;\n}",
        "TypeScript": "function findFirstAndLast(nums: number[], target: number): number[] {\n  function findFirst(nums: number[], target: number): number {\n    let leftIndex = 0;\n    let rightIndex = nums.length - 1;\n    let firstPosition = -1;\n\n    while (leftIndex <= rightIndex) {\n      const middleIndex = Math.floor((leftIndex + rightIndex) / 2);\n      const middleNumber = nums[middleIndex];\n\n      if (middleNumber < target) {\n        leftIndex = middleIndex + 1;\n      } else if (middleNumber > target) {\n        rightIndex = middleIndex - 1;\n      } else {\n        // Potential first occurrence, check left\n        firstPosition = middleIndex;\n\n        rightIndex = middleIndex - 1;\n      }\n    }\n\n    return firstPosition;\n  }\n\n  function findLast(nums: number[], target: number): number {\n    let leftIndex = 0;\n    let rightIndex = nums.length - 1;\n    let lastPosition = -1;\n\n    while (leftIndex <= rightIndex) {\n      const middleIndex = Math.floor((leftIndex + rightIndex) / 2);\n      const middleNumber = nums[middleIndex];\n\n      if (middleNumber < target) {\n        leftIndex = middleIndex + 1;\n      } else if (middleNumber > target) {\n        rightIndex = middleIndex - 1;\n      } else {\n        // Potential last occurrence, check right\n        lastPosition = middleIndex;\n\n        leftIndex = middleIndex + 1;\n      }\n    }\n\n    return lastPosition;\n  }\n\n  const firstOccurrence = findFirst(nums, target);\n  const lastOccurrence = findLast(nums, target);\n\n  return [firstOccurrence, lastOccurrence];\n}",
        "Java": "public class Solution {\n\n    public int[] searchRange(int[] nums, int target) {\n        int[] result = {-1, -1};\n        \n        result[0] = findFirst(nums, target);\n        result[1] = findLast(nums, target);\n        \n        return result;\n    }\n\n    private int findFirst(int[] nums, int target) {\n        int left = 0;\n        int right = nums.length - 1;\n        int firstPosition = -1;\n\n        while (left <= right) {\n            int middle = left + (right - left) / 2;\n            \n            if (nums[middle] < target) {\n                left = middle + 1;\n            } else if (nums[middle] > target) {\n                right = middle - 1;\n            } else {\n                // Potential first occurrence, keep searching left\n                firstPosition = middle;\n\n                right = middle - 1;\n\n            }\n        }\n        \n        return firstPosition;\n    }\n\n    private int findLast(int[] nums, int target) {\n        int left = 0;\n        int right = nums.length - 1;\n        int lastPosition = -1;\n\n        while (left <= right) {\n            int middle = left + (right - left) / 2;\n            \n            if (nums[middle] < target) {\n                left = middle + 1;\n            } else if (nums[middle] > target) {\n                right = middle - 1;\n            } else {\n                // Potential last occurrence, keep searching right\n                lastPosition = middle;\n\n                left = middle + 1;\n\n            }\n        }\n        \n        return lastPosition;\n    }\n}",
        "C++": "#include <vector>\n\nstd::vector<int> searchRange(std::vector<int>& nums, int target) {\n    std::vector<int> result = {-1, -1};\n    \n    if (nums.empty()) {\n        return result;\n    }\n\n    int left = 0;\n    int right = nums.size() - 1;\n\n    // Find the first position of the target\n    while (left <= right) {\n        int mid = left + (right - left) / 2;\n        \n        if (nums[mid] < target) {\n            left = mid + 1;\n        } else if (nums[mid] > target) {\n            right = mid - 1;\n        } else {\n            // Potential first occurence, check left side\n            result[0] = mid;\n\n            right = mid - 1;\n\n        }\n    }\n\n    left = 0;\n    right = nums.size() - 1;\n\n    // Find the last position of the target\n    while (left <= right) {\n        int mid = left + (right - left) / 2;\n        \n        if (nums[mid] < target) {\n            left = mid + 1;\n        } else if (nums[mid] > target) {\n            right = mid - 1;\n        } else {\n            // Potential last occurence, check right side\n            result[1] = mid;\n            left = mid + 1;\n        }\n    }\n\n    return result;\n}\n",
        "C#": "public class Solution\n{\n\tpublic int[] SearchRange(int[] nums, int target)\n\t{\n\t\tint[] result = { -1, -1 };\n\t\tif (nums == null || nums.Length == 0)\n\t\t{\n\t\t\treturn result;\n\t\t}\n\n\t\tresult[0] = FindFirstOccurrence(nums, target);\n\t\tresult[1] = FindLastOccurrence(nums, target);\n\n\t\treturn result;\n\t}\n\n\tprivate int FindFirstOccurrence(int[] nums, int target)\n\t{\n\t\tint leftIndex = 0;\n\t\tint rightIndex = nums.Length - 1;\n\t\tint firstOccurrence = -1;\n\n\t\twhile (leftIndex <= rightIndex)\n\t\t{\n\t\t\tint middleIndex = leftIndex + (rightIndex - leftIndex) / 2;\n\n\t\t\tif (nums[middleIndex] < target)\n\t\t\t{\n\t\t\t\tleftIndex = middleIndex + 1;\n\t\t\t}\n\t\t\telse if (nums[middleIndex] > target)\n\t\t\t{\n\t\t\t\trightIndex = middleIndex - 1;\n\t\t\t}\n\t\t\telse\n\t\t\t{\n\t\t\t\t// Found the target. Need to check left for first occurrence.\n\t\t\t\tfirstOccurrence = middleIndex;\n\n\t\t\t\trightIndex = middleIndex - 1;\n\n\t\t\t}\n\t\t}\n\n\t\treturn firstOccurrence;\n\t}\n\n\tprivate int FindLastOccurrence(int[] nums, int target)\n\t{\n\t\tint leftIndex = 0;\n\t\tint rightIndex = nums.Length - 1;\n\t\tint lastOccurrence = -1;\n\n\t\twhile (leftIndex <= rightIndex)\n\t\t{\n\t\t\tint middleIndex = leftIndex + (rightIndex - leftIndex) / 2;\n\n\t\t\tif (nums[middleIndex] < target)\n\t\t\t{\n\t\t\t\tleftIndex = middleIndex + 1;\n\t\t\t}\n\t\t\telse if (nums[middleIndex] > target)\n\t\t\t{\n\t\t\t\trightIndex = middleIndex - 1;\n\t\t\t}\n\t\t\telse\n\t\t\t{\n\t\t\t\t// Found the target. Need to check right for last occurrence.\n\t\t\t\tlastOccurrence = middleIndex;\n\n\t\t\t\tleftIndex = middleIndex + 1;\n\n\t\t\t}\n\t\t}\n\n\t\treturn lastOccurrence;\n\t}\n}\n",
        "Go": "package main\n\nfunc searchRange(numbers []int, target int) []int {\n\tfirstOccurrence := findFirstOccurrence(numbers, target)\n\tlastOccurrence := findLastOccurrence(numbers, target)\n\treturn []int{firstOccurrence, lastOccurrence}\n}\n\nfunc findFirstOccurrence(numbers []int, target int) int {\n\tleft, right := 0, len(numbers)-1\n\tfirstOccurrence := -1\n\n\tfor left <= right {\n\t\tmiddle := left + (right-left)/2\n\n\t\tif numbers[middle] < target {\n\t\t\tleft = middle + 1\n\t\t} else if numbers[middle] > target {\n\t\t\tright = middle - 1\n\t\t} else {\n\t\t\t// Potential first occurrence found\n\t\t\tfirstOccurrence = middle\n\t\t\t// Continue searching on the left side\n\t\t\tright = middle - 1\n\n\t\t}\n\t}\n\n\treturn firstOccurrence\n}\n\nfunc findLastOccurrence(numbers []int, target int) int {\n\tleft, right := 0, len(numbers)-1\n\tlastOccurrence := -1\n\n\tfor left <= right {\n\t\tmiddle := left + (right-left)/2\n\n\t\tif numbers[middle] < target {\n\t\t\tleft = middle + 1\n\t\t} else if numbers[middle] > target {\n\t\t\tright = middle - 1\n\t\t} else {\n\t\t\t// Potential last occurrence found\n\t\t\tlastOccurrence = middle\n\n\t\t\t// Continue searching on the right side\n\t\t\tleft = middle + 1\n\n\t\t}\n\t}\n\n\treturn lastOccurrence\n}\n",
        "Rust": "fn search_range(nums: Vec<i32>, target: i32) -> Vec<i32> {\n    let first_occurrence = find_first_occurrence(&nums, target);\n    let last_occurrence = find_last_occurrence(&nums, target);\n\n    vec![first_occurrence, last_occurrence]",
        "Kotlin": "fun findFirstAndLastPosition(numbers: IntArray, target: Int): IntArray {\n    val result = intArrayOf(-1, -1)\n    \n    // Find the first occurrence of the target\n    var leftIndex = 0\n    var rightIndex = numbers.size - 1\n    \n    while (leftIndex <= rightIndex) {\n      val middleIndex = leftIndex + (rightIndex - leftIndex) / 2\n      \n      if (numbers[middleIndex] < target) {\n        leftIndex = middleIndex + 1\n      } else if (numbers[middleIndex] > target) {\n        rightIndex = middleIndex - 1\n      } else {\n        //If target is found, search for the first occurrence\n        result[0] = middleIndex\n        rightIndex = middleIndex - 1\n      }\n    }\n    \n    // Find the last occurrence of the target\n    leftIndex = 0\n    rightIndex = numbers.size - 1\n    \n    while (leftIndex <= rightIndex) {\n      val middleIndex = leftIndex + (rightIndex - leftIndex) / 2\n      \n      if (numbers[middleIndex] < target) {\n        leftIndex = middleIndex + 1\n      } else if (numbers[middleIndex] > target) {\n        rightIndex = middleIndex - 1\n      } else {\n        //If target is found, search for the last occurrence\n        result[1] = middleIndex\n        leftIndex = middleIndex + 1\n      }\n    }\n    \n    return result\n  }",
        "Swift": "func findFirstAndLast(nums: [Int], target: Int) -> [Int] {\n    let firstPosition = findFirstPosition(nums: nums, target: target)\n    let lastPosition = findLastPosition(nums: nums, target: target)\n    return [firstPosition, lastPosition]\n}\n\nfunc findFirstPosition(nums: [Int], target: Int) -> Int {\n    var leftIndex = 0\n    var rightIndex = nums.count - 1\n    var firstPosition = -1\n\n    while leftIndex <= rightIndex {\n        let middleIndex = leftIndex + (rightIndex - leftIndex) / 2\n\n        if nums[middleIndex] < target {\n            leftIndex = middleIndex + 1\n        } else if nums[middleIndex] > target {\n            rightIndex = middleIndex - 1\n        } else {\n            // Found a match, but might not be the first\n            firstPosition = middleIndex\n\n            // Continue searching on the left side\n            rightIndex = middleIndex - 1\n        }\n    }\n\n    return firstPosition\n}\n\nfunc findLastPosition(nums: [Int], target: Int) -> Int {\n    var leftIndex = 0\n    var rightIndex = nums.count - 1\n    var lastPosition = -1\n\n    while leftIndex <= rightIndex {\n        let middleIndex = leftIndex + (rightIndex - leftIndex) / 2\n\n        if nums[middleIndex] < target {\n            leftIndex = middleIndex + 1\n        } else if nums[middleIndex] > target {\n            rightIndex = middleIndex - 1\n        } else {\n            // Found a match, but might not be the last\n            lastPosition = middleIndex\n\n            // Continue searching on the right side\n            leftIndex = middleIndex + 1\n        }\n    }\n\n    return lastPosition\n}\n",
        "Ruby": "def find_first_and_last_position(numbers, target):\n    first_position = find_first(numbers, target)\n    last_position = find_last(numbers, target)\n    return [first_position, last_position]\n\ndef find_first(numbers, target):\n    left_index = 0\n    right_index = len(numbers) - 1\n    first_occurrence = -1\n\n    while left_index <= right_index:\n        middle_index = (left_index + right_index) // 2\n\n        if numbers[middle_index] < target:\n            left_index = middle_index + 1\n        elsif numbers[middle_index] > target:\n            right_index = middle_index - 1\n        else:\n            # Potential first occurrence found.\n            first_occurrence = middle_index\n\n            # Keep searching on the left for the first.\n            right_index = middle_index - 1\n\n    return first_occurrence\n\ndef find_last(numbers, target):\n    left_index = 0\n    right_index = len(numbers) - 1\n    last_occurrence = -1\n\n    while left_index <= right_index:\n        middle_index = (left_index + right_index) // 2\n\n        if numbers[middle_index] < target:\n            left_index = middle_index + 1\n        elsif numbers[middle_index] > target:\n            right_index = middle_index - 1\n        else:\n            # Potential last occurrence found.\n            last_occurrence = middle_index\n\n            # Keep searching on the right for the last.\n            left_index = middle_index + 1\n\n    return last_occurrence",
        "PHP": "<?php\nfunction findFirstAndLastPosition(array $numbers, int $target) : array\n{\n    $firstPosition = findFirstPosition($numbers, $target);\n    $lastPosition = findLastPosition($numbers, $target);\n\n    return [$firstPosition, $lastPosition];\n}\n\nfunction findFirstPosition(array $numbers, int $target) : int\n{\n    $leftIndex = 0;\n    $rightIndex = count($numbers) - 1;\n    $firstPosition = -1;\n\n    while ($leftIndex <= $rightIndex) {\n        $middleIndex = $leftIndex + (int)(($rightIndex - $leftIndex) / 2);\n\n        if ($numbers[$middleIndex] < $target) {\n            $leftIndex = $middleIndex + 1;\n        } elseif ($numbers[$middleIndex] > $target) {\n            $rightIndex = $middleIndex - 1;\n        } else {\n            // Target found, but might not be the first occurrence.\n\n            $firstPosition = $middleIndex;\n\n            // Keep searching on the left side.\n\n            $rightIndex = $middleIndex - 1;\n        }\n    }\n\n    return $firstPosition;\n}\n\nfunction findLastPosition(array $numbers, int $target) : int\n{\n    $leftIndex = 0;\n    $rightIndex = count($numbers) - 1;\n    $lastPosition = -1;\n\n    while ($leftIndex <= $rightIndex) {\n        $middleIndex = $leftIndex + (int)(($rightIndex - $leftIndex) / 2);\n\n        if ($numbers[$middleIndex] < $target) {\n            $leftIndex = $middleIndex + 1;\n        } elseif ($numbers[$middleIndex] > $target) {\n            $rightIndex = $middleIndex - 1;\n        } else {\n            //Target found, but might not be the last occurrence.\n\n            $lastPosition = $middleIndex;\n\n            //Keep searching on the right side.\n\n            $leftIndex = $middleIndex + 1;\n        }\n    }\n\n    return $lastPosition;\n}\n\n?>"
      }
    }
  }
}
