{
  "id": "XOUfVbhMTXF70yWfIw9b",
  "slug": "merge-k-sorted-lists",
  "rank": 15,
  "title": "Merge k Sorted Lists",
  "difficulty": "hard",
  "topics": [
    "Linked List"
  ],
  "externalUrl": "https://www.leetcode.com/problems/merge-k-sorted-lists",
  "numViews": 1411,
  "questionBody": "<p>You are given an array of <code>k</code> linked-lists <code>lists</code>, each linked-list is sorted in ascending order.</p>\n\n<p><em>Merge all the linked-lists into one sorted linked-list and return it.</em></p>\n\n<p>&nbsp;</p>\n<p><strong class=\"example\">Example 1:</strong></p>\n\n<pre>\n<strong>Input:</strong> lists = [[1,4,5],[1,3,4],[2,6]]\n<strong>Output:</strong> [1,1,2,3,4,4,5,6]\n<strong>Explanation:</strong> The linked-lists are:\n[\n  1-&gt;4-&gt;5,\n  1-&gt;3-&gt;4,\n  2-&gt;6\n]\nmerging them into one sorted linked list:\n1-&gt;1-&gt;2-&gt;3-&gt;4-&gt;4-&gt;5-&gt;6\n</pre>\n\n<p><strong class=\"example\">Example 2:</strong></p>\n\n<pre>\n<strong>Input:</strong> lists = []\n<strong>Output:</strong> []\n</pre>\n\n<p><strong class=\"example\">Example 3:</strong></p>\n\n<pre>\n<strong>Input:</strong> lists = [[]]\n<strong>Output:</strong> []\n</pre>\n\n<p>&nbsp;</p>\n<p><strong>Constraints:</strong></p>\n\n<ul>\n\t<li><code>k == lists.length</code></li>\n\t<li><code>0 &lt;= k &lt;= 10<sup>4</sup></code></li>\n\t<li><code>0 &lt;= lists[i].length &lt;= 500</code></li>\n\t<li><code>-10<sup>4</sup> &lt;= lists[i][j] &lt;= 10<sup>4</sup></code></li>\n\t<li><code>lists[i]</code> is sorted in <strong>ascending order</strong>.</li>\n\t<li>The sum of <code>lists[i].length</code> will not exceed <code>10<sup>4</sup></code>.</li>\n</ul>\n",
  "clarifyingQuestions": [
    "What is the maximum number of linked lists (k) and what is the maximum length of each linked list in the input array?",
    "Can the linked lists contain negative values, zero, or just positive integers?",
    "Is it possible for the input list `lists` to be null or empty, and if so, what should the return value be in those cases?",
    "Are duplicate values allowed within and across the linked lists, and should they be preserved in the merged result?",
    "Is there a requirement for the merged linked list to be a new list, or can I modify the existing linked lists in place to create the merged list (avoiding creating new nodes if possible)?"
  ],
  "edgeCases": [
    {
      "edgeCase": "Empty input list (lists is null or empty)",
      "handling": "Return null immediately, as there are no lists to merge."
    },
    {
      "edgeCase": "Input list contains null or empty lists",
      "handling": "Treat null or empty lists as if they don't exist, skipping them during the merge process."
    },
    {
      "edgeCase": "All input lists are empty",
      "handling": "The merged list should be empty (return null) since all contributing lists are empty."
    },
    {
      "edgeCase": "Input list contains only one non-empty list",
      "handling": "Return the single non-empty list directly, as it's already sorted."
    },
    {
      "edgeCase": "Lists contain duplicate values within themselves and across different lists",
      "handling": "The solution should correctly handle duplicates, maintaining their order in the merged list."
    },
    {
      "edgeCase": "Lists contain a very large number of elements, approaching memory limits",
      "handling": "Consider a divide-and-conquer approach or using an iterative merge to avoid stack overflow or excessive memory consumption."
    },
    {
      "edgeCase": "Lists contain extremely large or small integer values",
      "handling": "Ensure integer overflow is handled appropriately, using `long` if necessary, depending on language constraints."
    },
    {
      "edgeCase": "One list is significantly longer than all others",
      "handling": "The algorithm's performance should not degrade significantly; priority queue based solutions handle this gracefully."
    }
  ],
  "companies": [
    {
      "slug": "amazon",
      "name": "Amazon",
      "logo": "/logos/amazon.jpg"
    },
    {
      "slug": "meta",
      "name": "Meta",
      "logo": "/logos/meta.jpg"
    },
    {
      "slug": "apple",
      "name": "Apple",
      "logo": "/logos/apple.jpg"
    },
    {
      "slug": "google",
      "name": "Google",
      "logo": "/logos/google.jpg"
    },
    {
      "slug": "microsoft",
      "name": "Microsoft",
      "logo": "/logos/microsoft.jpg"
    },
    {
      "slug": "oracle",
      "name": "Oracle",
      "logo": "/logos/oracle.jpg"
    },
    {
      "slug": "nvidia",
      "name": "NVIDIA",
      "logo": "/logos/nvidia.jpg"
    },
    {
      "slug": "tiktok",
      "name": "TikTok",
      "logo": "/logos/tiktok.png"
    },
    {
      "slug": "bloomberg-lp",
      "name": "Bloomberg",
      "logo": "/logos/bloomberg-lp.jpg"
    },
    {
      "slug": "snowflake",
      "name": "Snowflake",
      "logo": "/logos/snowflake.jpg"
    },
    {
      "slug": "uber",
      "name": "Uber",
      "logo": "/logos/uber.jpg"
    },
    {
      "slug": "citadel",
      "name": "Citadel",
      "logo": "/logos/citadel.jpg"
    },
    {
      "slug": "bytedance",
      "name": "ByteDance",
      "logo": "/logos/bytedance.jpg"
    },
    {
      "slug": "ebay",
      "name": "eBay",
      "logo": "/logos/ebay.jpg"
    },
    {
      "slug": "adobe",
      "name": "Adobe",
      "logo": "/logos/adobe.jpg"
    },
    {
      "slug": "walmart",
      "name": "Walmart",
      "logo": "/logos/walmart.jpg"
    },
    {
      "slug": "linkedin",
      "name": "LinkedIn",
      "logo": "/logos/linkedin.png"
    },
    {
      "slug": "two-sigma",
      "name": "Two Sigma",
      "logo": "/logos/two-sigma.jpg"
    },
    {
      "slug": "airbnb",
      "name": "Airbnb",
      "logo": "/logos/airbnb.jpg"
    },
    {
      "slug": "netapp",
      "name": "NetApp",
      "logo": "/logos/netapp.jpg"
    },
    {
      "slug": "sofi",
      "name": "SoFi",
      "logo": "/logos/sofi.jpg"
    },
    {
      "slug": "salesforce",
      "name": "Salesforce",
      "logo": "/logos/salesforce.png"
    },
    {
      "slug": "verkada",
      "name": "Verkada",
      "logo": "/logos/verkada.jpg"
    },
    {
      "slug": "rivian",
      "name": "Rivian",
      "logo": "/logos/rivian.jpg"
    },
    {
      "slug": "palantir",
      "name": "Palantir",
      "logo": "/logos/palantir.jpg"
    },
    {
      "slug": "anduril",
      "name": "Anduril",
      "logo": "/logos/anduril.jpg"
    },
    {
      "slug": "disney",
      "name": "Disney",
      "logo": "/logos/disney.jpg"
    },
    {
      "slug": "nutanix",
      "name": "Nutanix",
      "logo": "/logos/nutanix.jpg"
    },
    {
      "slug": "docusign",
      "name": "DocuSign",
      "logo": "/logos/docusign.jpg"
    },
    {
      "slug": "indeed",
      "name": "Indeed",
      "logo": "/logos/indeed.jpg"
    },
    {
      "slug": "samsung",
      "name": "Samsung",
      "logo": "/logos/samsung.jpg"
    },
    {
      "slug": "coupang",
      "name": "Coupang",
      "logo": "/logos/coupang.jpg"
    },
    {
      "slug": "hubspot",
      "name": "HubSpot",
      "logo": "/logos/hubspot.jpg"
    },
    {
      "slug": "mongodb",
      "name": "Mongodb",
      "logo": "/logos/mongodb.jpg"
    },
    {
      "slug": "doordash",
      "name": "DoorDash",
      "logo": "/logos/doordash.png"
    },
    {
      "slug": "cisco",
      "name": "Cisco",
      "logo": "/logos/cisco.jpg"
    },
    {
      "slug": "goldman-sachs",
      "name": "Goldman Sachs",
      "logo": "/logos/goldman-sachs.jpg"
    },
    {
      "slug": "dell",
      "name": "Dell",
      "logo": "/logos/dell.jpg"
    }
  ],
  "solutions": {
    "bruteForce": {
      "overview": "The brute force way to combine many sorted lists into one big sorted list is simple, but not very efficient. We basically dump everything into one big pile, then sort the whole pile from scratch. This ensures the final result is correctly sorted, but involves a lot of extra work.",
      "steps": [
        "First, take all the numbers from all the lists and put them into one single, big list.",
        "Now that you have one big list with all the numbers, completely forget that the original lists were sorted.",
        "Next, sort this entire big list from the smallest number to the largest number, using any sorting method.",
        "The result is a single, sorted list containing all the numbers from all the original lists, merged together."
      ],
      "bigO": {
        "runtimeValue": "O(N log N)",
        "runtimeExplanation": "The brute force approach first gathers all elements from the k sorted lists into a single list. If the total number of elements across all k lists is N, then this step takes O(N) time. The next step sorts this combined list of N elements. A typical efficient sorting algorithm like merge sort or quicksort has a time complexity of O(N log N). Therefore, the dominant operation is the sorting step, making the overall time complexity O(N log N).",
        "spaceValue": "O(N)",
        "spaceExplanation": "The brute force approach first gathers all elements from the k sorted lists into a single, big list. This requires creating a new list to store all N total elements, where N is the sum of the lengths of all input lists. Sorting this big list in-place might reduce auxiliary space, but this explanation doesn't specify in-place sorting. Therefore, the dominant space usage is the temporary list of size N, which leads to O(N) auxiliary space."
      },
      "languages": [
        "Python",
        "JavaScript",
        "TypeScript",
        "Java",
        "C++",
        "C#",
        "Go",
        "Rust",
        "Kotlin",
        "Swift",
        "Ruby",
        "PHP"
      ],
      "code": {
        "Python": "def merge_k_sorted_lists_brute_force(list_of_lists):\n    merged_list = []\n\n    # Combine all elements from all lists into a single list.\n    for sorted_list in list_of_lists:\n        merged_list.extend(sorted_list)\n\n    # Sort the combined list.\n    merged_list.sort()\n\n    return merged_list",
        "JavaScript": "function mergeKSortedListsBruteForce(listOfLists) {\n  const allElements = [];\n\n  // Gather all elements into a single array\n  for (let i = 0; i < listOfLists.length; i++) {\n    const currentList = listOfLists[i];\n    for (let j = 0; j < currentList.length; j++) {\n      allElements.push(currentList[j]);\n    }\n  }\n\n  // Now we sort the combined array\n\n  allElements.sort((a, b) => a - b);\n  // Sort the array to merge the lists.\n\n  return allElements;\n}\n",
        "TypeScript": "function mergeKSortedListsBruteForce(\n  lists: number[][]\n): number[] {\n  // Consolidate all lists into one.\n  let mergedList: number[] = [];\n  for (const individualList of lists) {\n    mergedList = mergedList.concat(individualList);\n  }\n\n  // Sorting the combined list is crucial to the result.\n  mergedList.sort((firstNumber, secondNumber) => firstNumber - secondNumber);\n\n  return mergedList;\n}\n",
        "Java": "class Solution {\n    public ListNode mergeKLists(ListNode[] lists) {\n        java.util.ArrayList<Integer> allValues = new java.util.ArrayList<>();\n\n        // Collect all values from all linked lists into a single list\n        for (ListNode head : lists) {\n            while (head != null) {\n                allValues.add(head.val);\n                head = head.next;\n            }\n        }\n\n        // Sort the combined list. This ensures all elements are ordered\n        java.util.Collections.sort(allValues);\n\n        ListNode dummyHead = new ListNode(0);\n        ListNode current = dummyHead;\n\n        // Build the final sorted linked list from the sorted values\n        for (int value : allValues) {\n            current.next = new ListNode(value);\n            current = current.next;\n        }\n\n        return dummyHead.next;\n    }\n}\n",
        "C++": "#include <iostream>\n#include <vector>\n#include <algorithm>\n\nstruct ListNode {\n    int val;\n    ListNode *next;\n    ListNode() : val(0), next(nullptr) {}\n    ListNode(int x) : val(x), next(nullptr) {}\n    ListNode(int x, ListNode *next) : val(x), next(next) {}\n};\n\nListNode* mergeKListsBruteForce(std::vector<ListNode*>& lists) {\n    std::vector<int> allValues;\n\n    // Collect all values from the k lists into a single vector\n    for (ListNode* head : lists) {\n        while (head != nullptr) {\n            allValues.push_back(head->val);\n            head = head->next;\n        }\n    }\n\n    // Sort the combined vector.\n    // This ensures all elements are in non-decreasing order\n    std::sort(allValues.begin(), allValues.end());\n\n    // Build the merged linked list from the sorted vector\n    ListNode* dummyHead = new ListNode(0);\n    ListNode* current = dummyHead;\n\n    for (int value : allValues) {\n        current->next = new ListNode(value);\n        current = current->next;\n    }\n\n    return dummyHead->next;\n}\n",
        "C#": "public class Solution\n{\n    public ListNode MergeKListsBruteForce(ListNode[] lists)\n    {\n        List<int> allValues = new List<int>();\n\n        // Collect all values from all lists\n        foreach (ListNode head in lists)\n        {\n            ListNode current = head;\n            while (current != null)\n            {\n                allValues.Add(current.val);\n                current = current.next;\n            }\n        }\n\n        // Sort the combined list\n        allValues.Sort();\n\n        // Construct the merged linked list\n        ListNode dummyHead = new ListNode(0);\n        ListNode currentResultNode = dummyHead;\n\n        // Construct the new list from sorted values\n        foreach (int value in allValues)\n        {\n            currentResultNode.next = new ListNode(value);\n            currentResultNode = currentResultNode.next;\n        }\n\n        //First node after dummy node is our answer\n        return dummyHead.next;\n    }\n}\n",
        "Go": "package main\n\nimport \"sort\"\n\n// ListNode definition\ntype ListNode struct {\n\tVal  int\n\tNext *ListNode\n}\n\nfunc mergeKListsBruteForce(lists []*ListNode) *ListNode {\n\tvar allValues []int\n\n\t// Collect all values from the linked lists.\n\tfor _, linkedList := range lists {\n\t\tfor linkedListNode := linkedList; linkedListNode != nil; linkedListNode = linkedListNode.Next {\n\t\t\tallValues = append(allValues, linkedListNode.Val)\n\t\t}\n\t}\n\n\t// Sort the combined list.\nsort.Ints(allValues)\n\n\t// Construct the merged linked list.\n\tif len(allValues) == 0 {\n\t\treturn nil\n\t}\n\n\thead := &ListNode{Val: allValues[0]}\n\tcurrent := head\n\n\t//Build result linked list\n\tfor i := 1; i < len(allValues); i++ {\n\t\tnewNode := &ListNode{Val: allValues[i]}\n\t\tcurrent.Next = newNode\n\t\tcurrent = newNode\n\t}\n\n\treturn head\n}\n",
        "Rust": "use std::cmp::Reverse;\n\nfn merge_k_sorted_lists_brute_force(lists: Vec<Vec<i32>>) -> Vec<i32> {\n    // Create a vector to hold all elements.\n    let mut all_numbers: Vec<i32> = Vec::new();\n\n    // Iterate through all the lists.\n    for current_list in lists {\n        // Add all the numbers to the single vector.\n        for &number in &current_list {\n            all_numbers.push(number);\n        }\n    }\n\n    // Sorting is needed to combine the lists in sorted order.\n    all_numbers.sort();\n\n    all_numbers\n}",
        "Kotlin": "fun mergeKSortedListsBruteForce(lists: List<List<Int>>): List<Int> {\n    val combinedList = mutableListOf<Int>()\n\n    // Collect all numbers from all lists\n    for (sortedList in lists) {\n        combinedList.addAll(sortedList)\n    }\n\n    // Now sort the combined list\n    combinedList.sort()\n\n    return combinedList\n}\n",
        "Swift": "func mergeKSortedListsBruteForce(lists: [[Int]]) -> [Int] {\n    var combinedList: [Int] = []\n\n    // Combine all the sublists into one\n    for sortedList in lists {\n        combinedList.append(contentsOf: sortedList)\n    }\n\n    // Sort the combined list using Swift's default sorting algorithm\n    combinedList.sort()\n\n    // Sorting is essential for the merge to be correct\n    return combinedList\n}\n",
        "Ruby": "def merge_k_sorted_lists_brute_force(list_of_lists)\n    # Combine all the elements from the k lists into one single list\n    combined_list = []\n    list_of_lists.each do |sorted_list|\n        combined_list.concat(sorted_list)\n    end\n\n    # Sort the combined list to merge all values from all k lists\n    combined_list.sort!\n\n    # Return the single sorted list containing all elements\n    return combined_list\nend",
        "PHP": "<?php\n\nfunction mergeKSortedListsBruteForce(array $listOfLists): array\n{\n    $mergedList = [];\n\n    //  Gather all the elements from each list into one big list.\n    foreach ($listOfLists as $list)\n    {\n        $mergedList = array_merge($mergedList, $list);\n    }\n\n    // Sort the entire merged list.  Losing original sort. \n    sort($mergedList);\n\n    // Need to reindex after sorting\n    $mergedList = array_values($mergedList);\n\n    return $mergedList;\n}\n\n?>"
      }
    },
    "optimal": {
      "overview": "We want to combine multiple sorted lists into one big sorted list as quickly as possible. The best way to do this is to use a 'divide and conquer' strategy, like organizing a tournament bracket. This approach breaks the big problem into smaller, easier-to-solve subproblems.",
      "steps": [
        "Pair up the lists and merge each pair into a single sorted list. This means combining two lists at a time in a way that keeps everything in order.",
        "Repeat this process, pairing up the newly merged lists and merging them again. Keep doing this until you have only one list left.",
        "Each merge step efficiently combines two sorted lists by repeatedly comparing the first elements of each list and picking the smaller one to add to the result. This is faster than other approaches because you only need to compare the beginning of each list.",
        "By repeatedly merging pairs of lists, we systematically reduce the number of lists until we arrive at the single, fully sorted result."
      ],
      "bigO": {
        "runtimeValue": "O(N log k)",
        "runtimeExplanation": "Let k be the number of sorted lists, and N be the total number of elements across all lists. The algorithm repeatedly merges pairs of lists. In each merge step, we are effectively processing all N elements once. Since we are halving the number of lists in each step (like a binary tree), there are log k merge steps. Therefore, the total time complexity is O(N log k), where N is the total number of elements and k is the number of lists.",
        "spaceValue": "O(N)",
        "spaceExplanation": "The algorithm merges lists in pairs, creating new sorted lists. In the worst case, during intermediate merge steps, we may need to hold lists containing close to all N elements (where N is the total number of elements across all k lists) before the final merged list is constructed. The space is used to store the newly merged lists at each step. Therefore, the auxiliary space used is proportional to the total number of elements N, leading to a space complexity of O(N)."
      },
      "languages": [
        "Python",
        "JavaScript",
        "TypeScript",
        "Java",
        "C++",
        "C#",
        "Go",
        "Rust",
        "Kotlin",
        "Swift",
        "Ruby",
        "PHP"
      ],
      "code": {
        "Python": "class ListNode:\n    def __init__(self, val=0, next=None):\n        self.val = val\n        self.next = next\n\ndef merge_k_lists(list_of_linked_lists):\n    if not list_of_linked_lists:\n        return None\n\n    while len(list_of_linked_lists) > 1:\n        merged_lists = []\n        for i in range(0, len(list_of_linked_lists), 2):\n            list1 = list_of_linked_lists[i]\n            list2 = list_of_linked_lists[i + 1] if (i + 1) < len(list_of_linked_lists) else None\n            \n            # Pair up the lists and merge them\n            merged_lists.append(merge_two_lists(list1, list2))\n\n        list_of_linked_lists = merged_lists\n\n    return list_of_linked_lists[0]\n\ndef merge_two_lists(list1, list2):\n    dummy_node = ListNode()\n    tail = dummy_node\n\n    # Iteratively pick the smaller node\n    while list1 and list2:\n        if list1.val < list2.val:\n            tail.next = list1\n            list1 = list1.next\n        else:\n            tail.next = list2\n            list2 = list2.next\n        tail = tail.next\n\n    # Append the remaining nodes, if any\n    if list1:\n        tail.next = list1\n    if list2:\n        tail.next = list2\n\n    return dummy_node.next\n",
        "JavaScript": "/**\n * Definition for singly-linked list.\n * function ListNode(val, next) {\n *     this.val = (val===undefined ? 0 : val)\n *     this.next = (next===undefined ? null : next)\n * }\n */\n/**\n * @param {ListNode[]} lists\n * @return {ListNode}\n */\nfunction mergeKLists(lists) {\n  if (!lists || lists.length === 0) {\n    return null;\n  }\n\n  // Repeatedly merge pairs until only one list remains\n  while (lists.length > 1) {\n    let mergedLists = [];\n    for (let i = 0; i < lists.length; i += 2) {\n      const list1 = lists[i];\n      const list2 = (i + 1 < lists.length) ? lists[i + 1] : null;\n\n      // Merge the two lists\n      mergedLists.push(mergeTwoLists(list1, list2));\n    }\n\n    // Update the lists array with the newly merged lists\n    lists = mergedLists;\n  }\n\n  return lists[0];\n\n  function mergeTwoLists(list1, list2) {\n    let dummyNode = { next: null };\n    let tail = dummyNode;\n\n    // Iterate while both lists have elements\n    while (list1 && list2) {\n\n      // Compare the current nodes and append the smaller one\n      if (list1.val <= list2.val) {\n        tail.next = list1;\n        list1 = list1.next;\n      } else {\n        tail.next = list2;\n        list2 = list2.next;\n      }\n      tail = tail.next;\n    }\n\n    // Append the remaining elements from either list\n    tail.next = list1 || list2;\n\n    return dummyNode.next;\n  }\n}\n",
        "TypeScript": "/**\n * Definition for singly-linked list.\n * class ListNode {\n *     val: number\n *     next: ListNode | null\n *     constructor(val?: number, next?: ListNode | null) {\n *         this.val = (val===undefined ? 0 : val)\n *         this.next = (next===undefined ? null : next)\n *     }\n * }\n */\n\nclass ListNode {\n    val: number\n    next: ListNode | null\n    constructor(val?: number, next?: ListNode | null) {\n        this.val = (val===undefined ? 0 : val)\n        this.next = (next===undefined ? null : next)\n    }\n}\n\nfunction mergeKLists(lists: Array<ListNode | null>): ListNode | null {\n\n    if (!lists || lists.length === 0) {\n        return null;\n    }\n\n    // Helper function to merge two sorted lists\n    function mergeTwoLists(list_one: ListNode | null, list_two: ListNode | null): ListNode | null {\n        let dummyNode = new ListNode(-1);\n        let tail = dummyNode;\n\n        while (list_one && list_two) {\n            if (list_one.val <= list_two.val) {\n                tail.next = list_one;\n                list_one = list_one.next;\n            } else {\n                tail.next = list_two;\n                list_two = list_two.next;\n            }\n            tail = tail.next;\n        }\n\n        tail.next = list_one || list_two;\n\n        return dummyNode.next;\n    }\n\n    // Iteratively merge pairs of lists until only one remains.\n    while (lists.length > 1) {\n        let mergedLists: (ListNode | null)[] = [];\n\n        // Pair up lists and merge them.\n        for (let i = 0; i < lists.length; i += 2) {\n\n            // Handle cases where an odd number of lists exist\n            let list_one = lists[i];\n            let list_two = (i + 1 < lists.length) ? lists[i + 1] : null;\n\n            // Merge the pair or keep the single list\n            let mergedList = mergeTwoLists(list_one, list_two);\n            mergedLists.push(mergedList);\n        }\n\n        // Update the lists array with the merged lists.\n        lists = mergedLists;\n    }\n\n    // The only list remaining is the fully merged list.\n    return lists[0] || null;\n}",
        "Java": "public class Solution {\n\tpublic ListNode mergeKLists(ListNode[] lists) {\n\t\tif (lists == null || lists.length == 0) {\n\t\t\treturn null;\n\t\t}\n\n\t\tint interval = 1;\n\t\t// Iteratively merge lists in pairs.\n\t\twhile (interval < lists.length) {\n\t\t\tfor (int i = 0; i < lists.length - interval; i += 2 * interval) {\n\t\t\t\tlists[i] = mergeTwoLists(lists[i], lists[i + interval]);\n\t\t\t}\n\t\t\tinterval *= 2;\n\t\t}\n\n\t\treturn lists[0];\n\t}\n\n\tprivate ListNode mergeTwoLists(ListNode list1, ListNode list2) {\n\t\tListNode dummyNode = new ListNode(0);\n\t\tListNode tail = dummyNode;\n\n\t\t// Compare elements from both lists and add the smaller one\n\t\twhile (list1 != null && list2 != null) {\n\t\t\tif (list1.val < list2.val) {\n\t\t\t\ttail.next = list1;\n\t\t\t\tlist1 = list1.next;\n\t\t\t} else {\n\t\t\t\ttail.next = list2;\n\t\t\t\tlist2 = list2.next;\n\t\t\t}\n\t\t\ttail = tail.next;\n\t\t}\n\n\t\t// Add the remaining elements from either list, if any.\n\t\tif (list1 != null) {\n\t\t\ttail.next = list1;\n\t\t}\n\n\t\tif (list2 != null) {\n\t\t\ttail.next = list2;\n\t\t}\n\n\t\treturn dummyNode.next;\n\t}\n\n\tpublic class ListNode {\n\t\tint val;\n\t\tListNode next;\n\n\t\tListNode() {}\n\n\t\tListNode(int val) {\n\t\t\tthis.val = val;\n\t\t}\n\n\t\tListNode(int val, ListNode next) {\n\t\t\tthis.val = val;\n\t\t\tthis.next = next;\n\t\t}\n\t}\n}\n",
        "C++": "#include <iostream>\n#include <vector>\n\nstruct ListNode {\n    int val;\n    ListNode *next;\n    ListNode(int x) : val(x), next(nullptr) {}\n};\n\nListNode* mergeTwoLists(ListNode* list1, ListNode* list2) {\n    ListNode dummyHead(0);\n    ListNode* tail = &dummyHead;\n\n    while (list1 && list2) {\n        if (list1->val < list2->val) {\n            tail->next = list1;\n            list1 = list1->next;\n        } else {\n            tail->next = list2;\n            list2 = list2->next;\n        }\n        tail = tail->next;\n    }\n\n    if (list1) {\n        tail->next = list1;\n    } else {\n        tail->next = list2;\n    }\n\n    return dummyHead.next;\n}\n\nListNode* mergeKLists(std::vector<ListNode*>& lists) {\n    if (lists.empty()) {\n        return nullptr;\n    }\n\n    // Repeatedly merge pairs until only one list remains.\n    while (lists.size() > 1) {\n        std::vector<ListNode*> mergedLists;\n        for (size_t i = 0; i < lists.size(); i += 2) {\n            // Merge pairs of lists\n            if (i + 1 < lists.size()) {\n                mergedLists.push_back(mergeTwoLists(lists[i], lists[i + 1]));\n            } else {\n                // Handle the case where there's an odd number of lists.\n                mergedLists.push_back(lists[i]);\n            }\n        }\n        lists = mergedLists;\n    }\n\n    // The remaining list is the merged result.\n    return lists[0];\n}",
        "C#": "using System;\nusing System.Collections.Generic;\n\npublic class Solution\n{\n    public class ListNode\n    {\n        public int val;\n        public ListNode next;\n        public ListNode(int val = 0, ListNode next = null)\n        {\n            this.val = val;\n            this.next = next;\n        }\n    }\n\n    public ListNode MergeKLists(ListNode[] lists)\n    {\n        if (lists == null || lists.Length == 0)\n        {\n            return null;\n        }\n\n        // Repeatedly merge lists until only one remains\n        while (lists.Length > 1)\n        {\n            List<ListNode> mergedLists = new List<ListNode>();\n            for (int i = 0; i < lists.Length; i += 2)\n            {\n                ListNode list1 = lists[i];\n                ListNode list2 = (i + 1 < lists.Length) ? lists[i + 1] : null;\n\n                // Merge pairs of lists\n                mergedLists.Add(MergeTwoLists(list1, list2));\n            }\n\n            lists = mergedLists.ToArray();\n        }\n\n        // The last remaining list is the fully merged list.\n        return lists[0];\n    }\n\n    private ListNode MergeTwoLists(ListNode list1, ListNode list2)\n    {\n        ListNode dummyHead = new ListNode();\n        ListNode current = dummyHead;\n\n        // Compare and merge until one list is empty\n        while (list1 != null && list2 != null)\n        {\n            if (list1.val < list2.val)\n            {\n                current.next = list1;\n                list1 = list1.next;\n            }\n            else\n            {\n                current.next = list2;\n                list2 = list2.next;\n            }\n            current = current.next;\n        }\n\n        // Append the remaining elements of list1, if any\n        if (list1 != null)\n        {\n            current.next = list1;\n        }\n\n        // Append the remaining elements of list2, if any\n        if (list2 != null)\n        {\n            current.next = list2;\n        }\n\n        return dummyHead.next;\n    }\n}\n",
        "Go": "package main\n\ntype ListNode struct {\n\tVal  int\n\tNext *ListNode\n}\n\nfunc mergeKLists(lists []*ListNode) *ListNode {\n\tif len(lists) == 0 {\n\t\treturn nil\n\t}\n\n\tfor len(lists) > 1 {\n\t\tmergedLists := []*ListNode{}\n\t\tfor i := 0; i < len(lists); i += 2 {\n\t\t\tif i+1 < len(lists) {\n\t\t\t\t// Merge pairs of lists\n\t\t\t\tmergedLists = append(mergedLists, mergeTwoLists(lists[i], lists[i+1]))\n\n\t\t\t} else {\n\t\t\t\t// If odd number of lists, add the last one\n\t\t\t\tmergedLists = append(mergedLists, lists[i])\n\t\t\t}\n\t\t}\n\t\tlists = mergedLists // Update list with merged lists\n\t}\n\n\treturn lists[0]\n}\n\nfunc mergeTwoLists(list1 *ListNode, list2 *ListNode) *ListNode {\n\tmergedListHead := &ListNode{}\n\tcurrent := mergedListHead\n\n\tfor list1 != nil && list2 != nil {\n\t\t// Compare the heads of the two lists.\n\t\tif list1.Val < list2.Val {\n\t\t\tcurrent.Next = list1\n\t\t\tlist1 = list1.Next\n\t\t} else {\n\t\t\tcurrent.Next = list2\n\t\t\tlist2 = list2.Next\n\t\t}\n\t\tcurrent = current.Next\n\t}\n\n\t// Append the remaining nodes from list1, if any\n\tif list1 != nil {\n\t\tcurrent.Next = list1\n\t}\n\n\t// Append the remaining nodes from list2, if any\n\tif list2 != nil {\n\t\tcurrent.Next = list2\n\t}\n\n\t// Return the head of the merged list\n\treturn mergedListHead.Next\n}\n",
        "Rust": "use std::cmp::Ordering;\nuse std::collections::BinaryHeap;\n\n#[derive(PartialEq, Eq, Debug)]\nstruct ListNode {\n  pub val: i32,\n  pub next: Option<Box<ListNode>>\n}\n\nimpl ListNode {\n  #[inline]\n  fn new(val: i32) -> Self {\n    ListNode {\n      next: None,\n      val\n    }\n  }\n}\n\nfn merge_k_lists(lists: Vec<Option<Box<ListNode>>>) -> Option<Box<ListNode>> {\n    let mut heap: BinaryHeap<(i32, *mut ListNode)> = BinaryHeap::new();\n\n    for mut list_option in lists {\n        if let Some(mut list_node) = list_option {\n            // Store the node value and a raw pointer to the node.\n            heap.push((list_node.val, &mut *list_node as *mut ListNode));\n        }\n    }\n\n    let mut dummy_head = Some(Box::new(ListNode::new(0)));\n    let mut current_node = dummy_head.as_mut();\n\n    while let Some((_, node_ptr)) = heap.pop() {\n        // Because BinaryHeap is a max-heap, reverse the ordering\n        let mut node = unsafe { Box::from_raw(node_ptr) };\n\n        if let Some(mut current) = current_node.take() {\n            current.next = Some(node);\n            current_node = current.next.as_mut();\n        }\n\n        if let Some(mut next_node) = (unsafe { (*node_ptr).next.take() }) {\n            // Push the next node onto the heap\n            heap.push((next_node.val, &mut *next_node as *mut ListNode));\n            (unsafe { (*node_ptr).next = Some(next_node) });\n        }\n        else{\n            (unsafe { (*node_ptr).next = None });\n        }\n\n    }\n\n    dummy_head.unwrap().next\n}\n",
        "Kotlin": "class ListNode(var `val`: Int) {\n    var next: ListNode? = null\n}\n\nfun mergeKLists(lists: Array<ListNode?>): ListNode? {\n    if (lists.isEmpty()) return null\n\n    var currentLists = lists.toList()\n\n    // Iteratively merge pairs of lists until only one remains\n    while (currentLists.size > 1) {\n        val mergedLists = mutableListOf<ListNode?>()\n        for (i in 0 until currentLists.size step 2) {\n            if (i + 1 < currentLists.size) {\n                // Merge pairs of lists\n                mergedLists.add(mergeTwoLists(currentLists[i], currentLists[i + 1]))\n            } else {\n                // If odd number of lists, add the last one\n                mergedLists.add(currentLists[i])\n            }\n        }\n        currentLists = mergedLists\n    }\n\n    return currentLists.firstOrNull()\n}\n\nfun mergeTwoLists(list1: ListNode?, list2: ListNode?): ListNode? {\n    val dummyNode = ListNode(-1)\n    var tail = dummyNode\n    var firstListHead = list1\n    var secondListHead = list2\n\n    // Compare nodes and build the sorted list\n    while (firstListHead != null && secondListHead != null) {\n        if (firstListHead.`val` < secondListHead.`val`) {\n            tail.next = firstListHead\n            firstListHead = firstListHead.next\n        } else {\n            tail.next = secondListHead\n            secondListHead = secondListHead.next\n        }\n        tail = tail.next!!\n    }\n\n    // Append remaining nodes, if any\n    if (firstListHead != null) {\n        tail.next = firstListHead\n    } else {\n        tail.next = secondListHead\n    }\n\n    return dummyNode.next\n}\n",
        "Swift": "class ListNode {\n    var value: Int\n    var next: ListNode?\n    init(_ value: Int) {\n        self.value = value\n        self.next = nil\n    }\n}\n\nfunc mergeKLists(lists: [ListNode?]) -> ListNode? {\n    var listOfLists = lists\n\n    // Repeatedly merge lists until only one remains\n    while listOfLists.count > 1 {\n        var mergedLists: [ListNode?] = []\n        for i in stride(from: 0, to: listOfLists.count, by: 2) {\n            if i + 1 < listOfLists.count {\n                // Merge pairs of lists\n                let mergedList = mergeTwoLists(list1: listOfLists[i], list2: listOfLists[i + 1])\n                mergedLists.append(mergedList)\n            } else {\n                // If there's an odd number of lists, add the last one as is\n                mergedLists.append(listOfLists[i])\n            }\n        }\n        listOfLists = mergedLists\n    }\n\n    return listOfLists.first ?? nil\n}\n\nfunc mergeTwoLists(list1: ListNode?, list2: ListNode?) -> ListNode? {\n    var firstList = list1\n    var secondList = list2\n    let dummyHead = ListNode(0)\n    var current = dummyHead\n\n    // Iterate until one of the lists is empty\n    while firstList != nil && secondList != nil {\n        // Comparing the first elements to find the smaller value.\n        if firstList!.value < secondList!.value {\n            current.next = firstList\n            firstList = firstList!.next\n        } else {\n            current.next = secondList\n            secondList = secondList!.next\n        }\n        current = current.next!\n    }\n\n    // Attach the remaining elements of the non-empty list\n    if firstList != nil {\n        current.next = firstList\n    }\n    if secondList != nil {\n        current.next = secondList\n    }\n\n    // Dummy head is needed to point to the begining of the list.\n    return dummyHead.next\n}",
        "Ruby": "def merge_k_sorted_lists(lists_of_nodes)\n  return [] if lists_of_nodes.nil? || lists_of_nodes.empty?\n\n  while lists_of_nodes.length > 1\n    merged_lists = []\n    i = 0\n\n    # Pairwise merge the lists\n    while i < lists_of_nodes.length\n      list1 = lists_of_nodes[i]\n      list2 = (i + 1 < lists_of_nodes.length) ? lists_of_nodes[i + 1] : nil\n\n      merged_lists << merge_two_lists(list1, list2)\n      i += 2\n    end\n\n    # Update the lists for the next iteration\n    lists_of_nodes = merged_lists\n  end\n\n  lists_of_nodes[0]\nend\n\ndef merge_two_lists(list1, list2)\n  return list2 if list1.nil?\n  return list1 if list2.nil?\n\n  dummy_node = ListNode.new(0)\n  tail = dummy_node\n\n  # Compare and merge nodes from both lists\n  while list1 && list2\n    if list1.val <= list2.val\n      tail.next = list1\n      list1 = list1.next\n    else\n      tail.next = list2\n      list2 = list2.next\n    end\n    tail = tail.next\n  end\n\n  # Append remaining nodes\n  tail.next = list1 if list1\n  tail.next = list2 if list2\n\n  dummy_node.next\nend\n\n# Definition for singly-linked list.\nclass ListNode\n  attr_accessor :val, :next\n  def initialize(val = 0, next_node = nil)\n    @val = val\n    @next = next_node\n  end\nend\n",
        "PHP": "<?php\n\n/**\n * Definition for singly-linked list.\n * class ListNode {\n *     public $val = 0;\n *     public $next = null;\n *     function __construct($val = 0, $next = null) {\n *         $this->val = $val;\n *         $this->next = $next;\n *     }\n * }\n */\nclass Solution {\n\n    /**\n     * @param ListNode[] $lists\n     * @return ListNode\n     */\n    function mergeKLists($lists) {\n        if (empty($lists)) {\n            return null;\n        }\n\n        while (count($lists) > 1) {\n            $mergedLists = [];\n\n            for ($i = 0; $i < count($lists); $i += 2) {\n                $list1 = $lists[$i];\n                $list2 = $i + 1 < count($lists) ? $lists[$i + 1] : null;\n\n                // Merge pairs of lists.\n                $mergedLists[] = $this->mergeTwoLists($list1, $list2);\n            }\n\n            $lists = $mergedLists;\n        }\n\n        return $lists[0];\n    }\n\n    private function mergeTwoLists($list1, $list2) {\n        $dummyNode = new ListNode();\n        $tail = $dummyNode;\n\n        // Iterate until one of the lists is empty\n        while ($list1 !== null && $list2 !== null) {\n            if ($list1->val < $list2->val) {\n                $tail->next = $list1;\n                $list1 = $list1->next;\n            } else {\n                $tail->next = $list2;\n                $list2 = $list2->next;\n            }\n            $tail = $tail->next;\n        }\n\n        // Attach the remaining elements of the non-empty list\n        $tail->next = $list1 !== null ? $list1 : $list2;\n\n        return $dummyNode->next;\n    }\n}\n"
      }
    }
  }
}
