{"id":"AwrahD9r0q6nWMNIGvTm","slug":"powx-n","rank":71,"title":"Pow(x, n)","difficulty":"medium","topics":["Recursion","Bit Manipulation"],"externalUrl":"https://www.leetcode.com/problems/powx-n","numViews":452,"questionBody":"<p>Implement <a href=\"http://www.cplusplus.com/reference/valarray/pow/\" target=\"_blank\">pow(x, n)</a>, which calculates <code>x</code> raised to the power <code>n</code> (i.e., <code>x<sup>n</sup></code>).</p>\n\n<p>&nbsp;</p>\n<p><strong class=\"example\">Example 1:</strong></p>\n\n<pre>\n<strong>Input:</strong> x = 2.00000, n = 10\n<strong>Output:</strong> 1024.00000\n</pre>\n\n<p><strong class=\"example\">Example 2:</strong></p>\n\n<pre>\n<strong>Input:</strong> x = 2.10000, n = 3\n<strong>Output:</strong> 9.26100\n</pre>\n\n<p><strong class=\"example\">Example 3:</strong></p>\n\n<pre>\n<strong>Input:</strong> x = 2.00000, n = -2\n<strong>Output:</strong> 0.25000\n<strong>Explanation:</strong> 2<sup>-2</sup> = 1/2<sup>2</sup> = 1/4 = 0.25\n</pre>\n\n<p>&nbsp;</p>\n<p><strong>Constraints:</strong></p>\n\n<ul>\n\t<li><code>-100.0 &lt; x &lt; 100.0</code></li>\n\t<li><code>-2<sup>31</sup> &lt;= n &lt;= 2<sup>31</sup>-1</code></li>\n\t<li><code>n</code> is an integer.</li>\n\t<li>Either <code>x</code> is not zero or <code>n &gt; 0</code>.</li>\n\t<li><code>-10<sup>4</sup> &lt;= x<sup>n</sup> &lt;= 10<sup>4</sup></code></li>\n</ul>\n","clarifyingQuestions":["What are the possible ranges for x and n? Can x be zero? Can n be negative?","If x is 0 and n is negative, what should I return? Should I throw an exception or return a specific value?","What level of precision is expected in the result? Is there a tolerance for rounding errors considering we are working with floating-point numbers?","Is 'n' guaranteed to be an integer, or should I handle the case where 'n' is a floating point number?","Are there any performance expectations or constraints I should be aware of, considering the potential range of 'n'?"],"edgeCases":[{"edgeCase":"x is 0 and n is negative","handling":"Return positive infinity (or throw an exception depending on language) as division by zero is undefined."},{"edgeCase":"x is 0 and n is 0","handling":"Return 1 as 0^0 is generally considered 1."},{"edgeCase":"n is Integer.MIN_VALUE (most negative integer)","handling":"Handle potential overflow when negating n by first converting x to its inverse if x!=1 and x!=-1 and then processing it correctly, or consider long."},{"edgeCase":"x is 1","handling":"Return 1 immediately as 1 raised to any power is 1."},{"edgeCase":"x is -1 and n is even","handling":"Return 1."},{"edgeCase":"x is -1 and n is odd","handling":"Return -1."},{"edgeCase":"n is 0","handling":"Return 1 as any number raised to the power of 0 is 1."},{"edgeCase":"Large x and n leading to potential floating point overflow","handling":"Be aware of the limits of floating point representation and the potential for overflow, and potentially suggest using logarithms and exponentiation if extremely large numbers are expected."}],"companies":[{"slug":"meta","name":"Meta","logo":"/logos/meta.jpg"},{"slug":"google","name":"Google","logo":"/logos/google.jpg"},{"slug":"amazon","name":"Amazon","logo":"/logos/amazon.jpg"},{"slug":"microsoft","name":"Microsoft","logo":"/logos/microsoft.jpg"},{"slug":"bloomberg-lp","name":"Bloomberg","logo":"/logos/bloomberg-lp.jpg"},{"slug":"apple","name":"Apple","logo":"/logos/apple.jpg"},{"slug":"yahoo","name":"Yahoo","logo":"/logos/yahoo.jpg"},{"slug":"linkedin","name":"LinkedIn","logo":"/logos/linkedin.png"},{"slug":"ebay","name":"eBay","logo":"/logos/ebay.jpg"},{"slug":"walmart","name":"Walmart","logo":"/logos/walmart.jpg"},{"slug":"citadel","name":"Citadel","logo":"/logos/citadel.jpg"},{"slug":"adobe","name":"Adobe","logo":"/logos/adobe.jpg"},{"slug":"tiktok","name":"TikTok","logo":"/logos/tiktok.png"},{"slug":"qualcomm","name":"Qualcomm","logo":"/logos/qualcomm.jpg"},{"slug":"salesforce","name":"Salesforce","logo":"/logos/salesforce.png"},{"slug":"uber","name":"Uber","logo":"/logos/uber.jpg"},{"slug":"goldman-sachs","name":"Goldman Sachs","logo":"/logos/goldman-sachs.jpg"},{"slug":"servicenow","name":"ServiceNow","logo":"/logos/servicenow.jpg"}],"solutions":{"bruteForce":{"overview":"The basic idea is to repeatedly multiply the base number by itself. If the exponent is positive, we just multiply the base that many times. If the exponent is negative, we'll deal with that case separately, because it is equivalent to one divided by the base raised to the positive exponent.","steps":["First, consider the case where the exponent is zero. In that case, the result is always one.","If the exponent is positive, start with the base number.","Multiply the base number by itself, and keep doing this repeatedly. Each time, reduce the exponent value by one, tracking the number of multiplications.","Continue this process until the exponent reaches zero.","The final result is the accumulated product after all these multiplications.","If the exponent is negative, calculate the base raised to the positive absolute value of the exponent using the above procedure.","Then, take one and divide it by the result obtained in the previous step. This gives the correct answer for a negative exponent."],"bigO":{"runtimeValue":"O(n)","runtimeExplanation":"The provided solution iterates a number of times equal to the absolute value of the exponent n. When n is positive, there is a single loop that multiplies the base by itself n times. When n is negative, the absolute value of n is taken, and then there is a single loop that multiplies the base by itself abs(n) times. Therefore, the number of operations grows linearly with the input n, and the time complexity is O(n).","spaceValue":"O(1)","spaceExplanation":"The plain English explanation outlines an iterative process involving multiplication and decrementing the exponent. It only requires storing a few variables like the base, the exponent (which is decremented), and the accumulated product. No auxiliary data structures such as lists, arrays, or hash maps are used. The memory needed for these variables remains constant irrespective of the size of the exponent (n), therefore the auxiliary space complexity is O(1)."},"languages":["Python","JavaScript","TypeScript","Java","C++","C#","Go","Rust","Kotlin","Swift","Ruby","PHP"],"code":{"Python":"def power_function_brute_force(base, exponent):\n    if exponent == 0:\n        return 1\n\n    if exponent > 0:\n        result = base\n\n        # Repeatedly multiply the base by itself\n        exponent_tracker = exponent - 1\n\n        while exponent_tracker > 0:\n            result *= base\n            exponent_tracker -= 1\n\n        return result\n\n    # Account for negative exponent by taking reciprocal\n    else:\n        positive_exponent = abs(exponent)\n        result = base\n\n        # Repeatedly multiply the base by itself\n        exponent_tracker = positive_exponent - 1\n\n        while exponent_tracker > 0:\n            result *= base\n            exponent_tracker -= 1\n\n        # Invert result for negative powers\n        return 1 / result\n","JavaScript":"function power(base, exponent) {\n  if (exponent === 0) {\n    return 1;\n  }\n\n  if (exponent > 0) {\n    let result = base;\n    let exponentCounter = exponent - 1;\n\n    // Repeatedly multiply the base while decrementing the exponent.\n\n    while (exponentCounter > 0) {\n      result *= base;\n      exponentCounter--;\n    }\n    return result;\n  } else {\n    // Handle negative exponents by inverting the result of the positive exponent.\n\n    const absoluteExponent = Math.abs(exponent);\n    let positivePowerResult = base;\n    let exponentCounter = absoluteExponent - 1;\n\n    while (exponentCounter > 0) {\n      positivePowerResult *= base;\n      exponentCounter--;\n    }\n\n    // Avoid division by zero.\n\n    return 1 / positivePowerResult;\n  }\n}\n","TypeScript":"function myPowBruteForce(base: number, exponent: number): number {\n  if (exponent === 0) {\n    return 1;\n  }\n\n  let absoluteExponent = Math.abs(exponent);\n  let result = base;\n\n  // Handle positive exponent\n  if (exponent > 0) {\n    for (let multiplicationCount = 1; multiplicationCount < exponent; multiplicationCount++) {\n      result *= base;\n    }\n  } else {\n    // When exponent is negative, calculate positive power first\n    for (let multiplicationCount = 1; multiplicationCount < absoluteExponent; multiplicationCount++) {\n      result *= base;\n    }\n\n    // Take the inverse to account for the negative exponent\n    result = 1 / result;\n  }\n\n  return result;\n}","Java":"public class Solution {\n    public double myPow(double base, int exponent) {\n        // Base case: Anything raised to the power of 0 is 1\n        if (exponent == 0) {\n            return 1;\n        }\n\n        if (exponent > 0) {\n            double result = 1.0;\n            int exponentCounter = exponent;\n\n            // Repeatedly multiply the base by itself 'exponent' number of times\n            while (exponentCounter > 0) {\n                result *= base;\n                exponentCounter--;\n            }\n            return result;\n        } else {\n            // Need to handle negative exponents\n            int absoluteExponent = Math.abs(exponent);\n            double positivePower = 1.0;\n            int exponentCounter = absoluteExponent;\n\n            //Calculate x to the power of the positive absolute exponent\n            while (exponentCounter > 0) {\n                positivePower *= base;\n                exponentCounter--;\n            }\n\n            // For negative exponents, return 1 divided by x to the power of the absolute exponent\n            return 1.0 / positivePower;\n        }\n    }\n}\n","C++":"double power(double base, int exponent) {\n    if (exponent == 0) {\n        return 1.0;\n    }\n\n    // Handle positive exponent\n    if (exponent > 0) {\n        double result = base;\n        int exponentCounter = exponent - 1;\n\n        while (exponentCounter > 0) {\n            result *= base;\n            exponentCounter--;\n        }\n\n        return result;\n    }\n\n    // Handle negative exponent\n    else {\n        // Calculate the power for the positive exponent\n        int positiveExponent = -exponent;\n        double positivePowerResult = base;\n        int exponentCounter = positiveExponent - 1;\n\n        while (exponentCounter > 0) {\n            positivePowerResult *= base;\n            exponentCounter--;\n        }\n\n        //Invert the result because the original exponent was negative\n        return 1.0 / positivePowerResult;\n    }\n}","C#":"public class Solution\n{\n    public double MyPow(double baseNumber, int exponent)\n    {\n        if (exponent == 0)\n        {\n            return 1;\n        }\n\n        double result = 1.0;\n        bool isNegativeExponent = exponent < 0;\n\n        if (isNegativeExponent)\n        {\n            exponent = Math.Abs(exponent);\n        }\n\n        // Multiply base by itself, tracking multiplications\n        for (int i = 0; i < exponent; i++)\n        {\n            result *= baseNumber;\n        }\n\n        // Handle negative exponents by taking the reciprocal\n        if (isNegativeExponent)\n        {\n\n            result = 1.0 / result;\n\n        }\n\n        return result;\n    }\n}\n","Go":"package main\n\nfunc myPow(base float64, exponent int) float64 {\n\t// Handle the case where the exponent is zero; anything to the power of 0 is 1.\n\tif exponent == 0 {\n\t\treturn 1.0\n\t}\n\n\tif exponent > 0 {\n\t\tresult := base\n\t\texponentCounter := exponent - 1\n\t\tfor exponentCounter > 0 {\n\t\t\tresult *= base\n\t\t\texponentCounter--\n\t\t}\n\t\treturn result\n\t}\n\n\t// For negative exponents, calculate the reciprocal of the positive exponent result.\n\tpositiveExponent := -exponent\n\tresult := base\n\texponentCounter := positiveExponent - 1\n\tfor exponentCounter > 0 {\n\t\tresult *= base\n\t\texponentCounter--\n\t}\n\n\t// Calculating the reciprocal to handle negative exponent\n\treturn 1.0 / result\n}","Rust":"pub fn pow_brute_force(base: f64, exponent: i32) -> f64 {\n    if exponent == 0 {\n        return 1.0;\n    }\n    let mut accumulated_product = base;\n    let mut exponent_value = exponent.abs();\n    // Handle the positive exponent case\n    if exponent > 0 {\n        while exponent_value > 1 {\n            accumulated_product *= base;\n            exponent_value -= 1;\n        }\n        return accumulated_product;\n    } else {\n\n        // To avoid integer overflow for i32::MIN\n        if exponent == i32::MIN {\n            accumulated_product = pow_brute_force(base, i32::MAX);\n            return 1.0 / (accumulated_product * base);\n        }\n\n        // For negative exponents, calculate the positive power first.\n        accumulated_product = pow_brute_force(base, exponent.abs());\n\n        //Then take its reciprocal to get the result.\n        return 1.0 / accumulated_product;\n    }\n}","Kotlin":"fun powerBruteForce(base: Double, exponent: Int): Double {\n    if (exponent == 0) {\n        return 1.0\n    }\n\n    var absoluteExponent = exponent\n    var result = base\n\n    // Handle negative exponents\n    if (exponent < 0) {\n        absoluteExponent = -exponent\n    }\n\n    // Calculate base raised to the absolute exponent.\n    for (i in 1 until absoluteExponent) {\n        result *= base\n    }\n\n    // Need to handle the edge case of negative exponents\n    if (exponent < 0) {\n\n        return 1.0 / result\n\n    }\n\n    return result\n}","Swift":"func powerBruteForce(base: Double, exponent: Int) -> Double {\n    // Handle the base case where the exponent is zero.\n    if exponent == 0 {\n        return 1.0\n    }\n\n    if exponent > 0 {\n        var result = base\n        var currentExponent = exponent\n\n        // Multiply the base by itself 'exponent' times\n        while currentExponent > 1 {\n            result *= base\n            currentExponent -= 1\n        }\n\n        return result\n    } else {\n        // For negative exponents, use the identity x^-n = 1 / x^n.\n        let positiveExponent = abs(exponent)\n        var result = base\n        var currentExponent = positiveExponent\n\n        while currentExponent > 1 {\n            result *= base\n            currentExponent -= 1\n        }\n\n        // Invert the result for the negative exponent.\n        return 1.0 / result\n    }\n}","Ruby":"def power(base, exponent):\n    # Handle the base case where the exponent is zero\n    if exponent == 0:\n        return 1\n\n    if exponent > 0:\n        result = base\n        exponent_count = exponent - 1\n\n        while exponent_count > 0:\n            result = result * base\n            exponent_count = exponent_count - 1\n\n        return result\n    else:\n        # Calculate power for positive exponent and invert\n        positive_exponent = -exponent\n        result = base\n        exponent_count = positive_exponent - 1\n\n        while exponent_count > 0:\n            result = result * base\n            exponent_count = exponent_count - 1\n\n        # Negative exponents require division\n        return 1.0 / result\n","PHP":"<?php\nfunction power(float $baseNumber, int $exponent):\nfloat {\n    if ($exponent == 0) {\n        return 1.0;\n    }\n\n    $absoluteExponent = abs($exponent);\n    $result = $baseNumber;\n\n    // Handle positive exponent by repeated multiplication.\n\n    if ($exponent > 0) {\n        $exponentCounter = 1;\n\n        while ($exponentCounter < $exponent) {\n            $result *= $baseNumber;\n            $exponentCounter++;\n        }\n    } else {\n        // Store the result from the positive exponent.\n\n        $exponentCounter = 1;\n\n        while ($exponentCounter < $absoluteExponent) {\n            $result *= $baseNumber;\n            $exponentCounter++;\n        }\n\n        // For a negative exponent, return 1 / (base ^ abs(exponent)).\n        $result = 1.0 / $result;\n    }\n\n    return $result;\n}\n\n?>"}},"optimal":{"overview":"Calculating a number to a large power by multiplying it repeatedly is slow. A faster way involves repeatedly squaring the base and adjusting the exponent, effectively reducing the number of multiplication operations required.","steps":["First, handle the simple cases: if the exponent is zero, the answer is always one. If the exponent is one, the answer is just the original number.","If the exponent is negative, take the inverse of the base and make the exponent positive. We will eventually compute this positive power, so dealing with the negative now will simplify calculations later.","Now, repeatedly square the base and halve the exponent. If the exponent is odd at any point, multiply the current result by the current base. By halving, we mean integer division, discarding any remainder.","Continue squaring the base and halving the exponent until the exponent reaches zero. At that point, the accumulated result is the answer."],"bigO":{"runtimeValue":"O(log n)","runtimeExplanation":"The dominant operation in this algorithm is repeatedly halving the exponent n in each iteration of the loop. Each division by 2 brings the exponent closer to zero. The number of times the exponent can be halved before reaching zero is logarithmic with respect to the initial value of n. Therefore, the time complexity is O(log n).","spaceValue":"O(1)","spaceExplanation":"The algorithm uses a constant amount of extra space. It stores a few variables to keep track of the base, exponent, and the intermediate result. The space required by these variables does not depend on the size of the exponent (n) or the base (x). Therefore, the auxiliary space complexity is O(1)."},"languages":["Python","JavaScript","TypeScript","Java","C++","C#","Go","Rust","Kotlin","Swift","Ruby","PHP"],"code":{"Python":"def power(base, exponent):\n    if exponent == 0:\n        return 1\n    \n    if exponent == 1:\n        return base\n\n    # Handle negative exponents by inverting the base.\n    if exponent < 0:\n        base = 1 / base\n        exponent = -exponent\n\n    result = 1\n\n    # Repeatedly square the base and halve the exponent.\n    while exponent > 0:\n        # If the exponent is odd, multiply the result by the base.\n        if exponent % 2 == 1:\n            result *= base\n\n        base *= base\n\n        # Integer division to halve the exponent.\n        exponent //= 2\n\n    return result","JavaScript":"function myPow(baseNumber, exponent) {\n  if (exponent === 0) {\n    return 1;\n  }\n\n  if (exponent === 1) {\n    return baseNumber;\n  }\n\n  // Handle negative exponents by inverting the base.\n  if (exponent < 0) {\n    baseNumber = 1 / baseNumber;\n    exponent = -exponent;\n  }\n\n  let result = 1;\n  let currentBase = baseNumber;\n  let currentExponent = exponent;\n\n  // Iteratively square the base and halve the exponent.\n  while (currentExponent > 0) {\n    // If exponent is odd, multiply the result by the current base.\n    if (currentExponent % 2 === 1) {\n      result *= currentBase;\n    }\n\n    // Square the base for the next iteration.\n    currentBase *= currentBase;\n\n    // Integer division to halve the exponent.\n    currentExponent = Math.floor(currentExponent / 2);\n  }\n\n  return result;\n}","TypeScript":"function power(baseNumber: number, exponent: number): number {\n  if (exponent === 0) {\n    return 1;\n  }\n\n  if (exponent === 1) {\n    return baseNumber;\n  }\n\n  // Handle negative exponents by inverting the base.\n  if (exponent < 0) {\n    baseNumber = 1 / baseNumber;\n    exponent = -exponent;\n  }\n\n  let result = 1;\n  let currentBase = baseNumber;\n  let currentExponent = exponent;\n\n  while (currentExponent > 0) {\n    // If the exponent is odd, multiply the result by the current base.\n    if (currentExponent % 2 === 1) {\n      result *= currentBase;\n    }\n\n    // Square the base and halve the exponent.\n    currentBase *= currentBase;\n    currentExponent = Math.floor(currentExponent / 2);\n  }\n\n  return result;\n}","Java":"public class Solution {\n    public double myPow(double base, int exponent) {\n        if (exponent == 0) {\n            return 1;\n        }\n        if (exponent == 1) {\n            return base;\n        }\n\n        // Handle negative exponents by inverting the base\n        if (exponent < 0) {\n\t\t\tbase = 1 / base;\n\t\t\texponent = -exponent;\n        }\n\n        double result = 1;\n        double currentBase = base;\n        int currentExponent = exponent;\n\n        while (currentExponent > 0) {\n            // If the exponent is odd, multiply the result by the current base\n            if (currentExponent % 2 == 1) {\n\t\t\t\tresult = result * currentBase;\n            }\n\n            // Square the base and halve the exponent\n            currentBase = currentBase * currentBase;\n\t\t\t// Integer division discards the remainder\n            currentExponent = currentExponent / 2;\n        }\n\n        return result;\n    }\n}","C++":"double power(double base, int exponent) {\n    if (exponent == 0) {\n      return 1.0;\n    }\n\n    if (exponent == 1) {\n      return base;\n    }\n\n    // Handle negative exponents by inverting the base\n    if (exponent < 0) {\n\n      base = 1.0 / base;\n      exponent = -exponent;\n    }\n\n    double result = 1.0;\n    double currentBase = base;\n    int currentExponent = exponent;\n\n    // Repeatedly square the base and halve the exponent.\n    while (currentExponent > 0) {\n\n      //If exponent is odd, multiply result by the current base\n      if (currentExponent % 2 != 0) {\n        result *= currentBase;\n      }\n\n      currentBase *= currentBase;\n      currentExponent /= 2;\n    }\n\n    return result;\n  }","C#":"public class Solution\n{\n    public double MyPow(double x, int n)\n    {\n        // Handle base cases of exponentiation.\n        if (n == 0)\n        {\n            return 1;\n        }\n        if (n == 1)\n        {\n            return x;\n        }\n\n        // If the exponent is negative, invert the base\n        // and convert the exponent to positive.\n        if (n < 0)\n        {\n            x = 1 / x;\n            n = -n;\n        }\n\n        double result = 1;\n        double currentBase = x;\n        int exponent = n;\n\n        while (exponent > 0)\n        {\n            // If the exponent is odd, we multiply the result\n            // by the current base.\n            if (exponent % 2 == 1)\n            {\n                result *= currentBase;\n\n            }\n\n            // Square the base and halve the exponent for\n            // subsequent iterations. We are using integer division.\n            currentBase *= currentBase;\n            exponent /= 2;\n        }\n\n        return result;\n    }\n}","Go":"package main\n\nfunc myPow(base float64, exponent int) float64 {\n\tif exponent == 0 {\n\t\treturn 1\n\t}\n\n\tif exponent == 1 {\n\t\treturn base\n\t}\n\n\tif exponent < 0 {\n\t\tbase = 1 / base\n\t\texponent = -exponent\n\t}\n\n\tresult := 1.0\n\n\tfor exponent > 0 {\n\t\t// If exponent is odd, include the base once\n\t\tif exponent%2 == 1 {\n\t\t\tresult *= base\n\n\t\t}\n\n\t\t// Square the base to efficiently handle powers of 2\n\t\tbase *= base\n\n\t\t// Integer division to reduce the exponent\n\t\texponent /= 2\n\n\t}\n\n\treturn result\n}","Rust":"fn power(base: f64, exponent: i32) -> f64 {\n    if exponent == 0 {\n      return 1.0;\n    }\n\n    if exponent == 1 {\n      return base;\n    }\n\n    let mut base_value = base;\n    let mut exponent_value = exponent;\n    let mut result = 1.0;\n\n    // Handle negative exponents by inverting the base.\n    if exponent_value < 0 {\n      base_value = 1.0 / base_value;\n      exponent_value = -exponent_value;\n    }\n\n    while exponent_value > 0 {\n      // If the exponent is odd, multiply the result by the base.\n      if exponent_value % 2 == 1 {\n\n        result *= base_value;\n      }\n\n      // Square the base and halve the exponent.\n      base_value *= base_value;\n      exponent_value /= 2;\n    }\n\n    result\n  }","Kotlin":"fun power(base: Double, exponent: Int): Double {\n    if (exponent == 0) {\n        return 1.0\n    }\n\n    if (exponent == 1) {\n        return base\n    }\n\n    var workingBase = base\n    var workingExponent = exponent\n\n    // If negative exponent, invert base and make exponent positive\n    if (workingExponent < 0) {\n\n        workingBase = 1.0 / workingBase\n\n        workingExponent = -workingExponent\n    }\n\n    var result = 1.0\n\n    //Repeatedly square the base and halve the exponent\n    while (workingExponent > 0) {\n\n        // If exponent is odd, multiply result by current base\n        if (workingExponent % 2 != 0) {\n            result *= workingBase\n        }\n\n        // Square the base for the next iteration\n        workingBase *= workingBase\n\n        //Integer division to halve the exponent\n        workingExponent /= 2\n\n    }\n\n    return result\n}","Swift":"func power(base: Double, exponent: Int) -> Double {\n    if exponent == 0 {\n        return 1.0\n    }\n\n    if exponent == 1 {\n        return base\n    }\n\n    var currentBase = base\n    var currentExponent = exponent\n\n    // Handle negative exponents by inverting the base.\n    if currentExponent < 0 {\n        currentBase = 1.0 / currentBase\n        currentExponent = -currentExponent\n    }\n\n    var result: Double = 1.0\n\n    // Repeatedly square the base and halve the exponent.\n    while currentExponent > 0 {\n\n        // If exponent is odd, multiply result by current base\n        if currentExponent % 2 == 1 {\n            result *= currentBase\n        }\n\n        currentBase *= currentBase\n\n        // Integer division to halve the exponent.\n        currentExponent /= 2\n    }\n\n    return result\n}","Ruby":"def power(base_number, exponent):\n    if exponent == 0:\n        return 1\n\n    if exponent == 1:\n        return base_number\n\n    # If the exponent is negative, invert the base and make the exponent positive.\n    if exponent < 0:\n        base_number = 1 / base_number\n        exponent = -exponent\n\n    result = 1\n    current_base = base_number\n    current_exponent = exponent\n\n    while current_exponent > 0:\n        # If the exponent is odd, multiply the result by the current base.\n        if current_exponent % 2 == 1:\n            result *= current_base\n\n        # Square the base and halve the exponent for the next iteration.\n        current_base *= current_base\n        current_exponent //= 2\n\n    return result","PHP":"<?php\nfunction power(float $base, int $exponent): float {\n    if ($exponent == 0) {\n        return 1.0;\n    }\n\n    if ($exponent == 1) {\n        return $base;\n    }\n\n    // If the exponent is negative, invert the base and make the exponent positive.\n    if ($exponent < 0) {\n        $base = 1 / $base;\n        $exponent = -$exponent;\n\n    }\n\n    $result = 1.0;\n    $currentBase = $base;\n    $currentExponent = $exponent;\n\n    while ($currentExponent > 0) {\n        // If the exponent is odd, multiply the result by the current base.\n        if ($currentExponent % 2 == 1) {\n            $result *= $currentBase;\n\n        }\n\n        // Square the base and halve the exponent.\n        $currentBase *= $currentBase;\n        $currentExponent = intdiv($currentExponent, 2);\n\n    }\n\n    return $result;\n}\n?>"}}}}