{"id":"pixb4tp6TWYBttmNIJOy","slug":"roman-to-integer","rank":27,"title":"Roman to Integer","difficulty":"easy","topics":["Strings"],"externalUrl":"https://www.leetcode.com/problems/roman-to-integer","numViews":899,"questionBody":"<p>Roman numerals are represented by seven different symbols:&nbsp;<code>I</code>, <code>V</code>, <code>X</code>, <code>L</code>, <code>C</code>, <code>D</code> and <code>M</code>.</p>\n\n<pre>\n<strong>Symbol</strong>       <strong>Value</strong>\nI             1\nV             5\nX             10\nL             50\nC             100\nD             500\nM             1000</pre>\n\n<p>For example,&nbsp;<code>2</code> is written as <code>II</code>&nbsp;in Roman numeral, just two ones added together. <code>12</code> is written as&nbsp;<code>XII</code>, which is simply <code>X + II</code>. The number <code>27</code> is written as <code>XXVII</code>, which is <code>XX + V + II</code>.</p>\n\n<p>Roman numerals are usually written largest to smallest from left to right. However, the numeral for four is not <code>IIII</code>. Instead, the number four is written as <code>IV</code>. Because the one is before the five we subtract it making four. The same principle applies to the number nine, which is written as <code>IX</code>. There are six instances where subtraction is used:</p>\n\n<ul>\n\t<li><code>I</code> can be placed before <code>V</code> (5) and <code>X</code> (10) to make 4 and 9.&nbsp;</li>\n\t<li><code>X</code> can be placed before <code>L</code> (50) and <code>C</code> (100) to make 40 and 90.&nbsp;</li>\n\t<li><code>C</code> can be placed before <code>D</code> (500) and <code>M</code> (1000) to make 400 and 900.</li>\n</ul>\n\n<p>Given a roman numeral, convert it to an integer.</p>\n\n<p>&nbsp;</p>\n<p><strong class=\"example\">Example 1:</strong></p>\n\n<pre>\n<strong>Input:</strong> s = &quot;III&quot;\n<strong>Output:</strong> 3\n<strong>Explanation:</strong> III = 3.\n</pre>\n\n<p><strong class=\"example\">Example 2:</strong></p>\n\n<pre>\n<strong>Input:</strong> s = &quot;LVIII&quot;\n<strong>Output:</strong> 58\n<strong>Explanation:</strong> L = 50, V= 5, III = 3.\n</pre>\n\n<p><strong class=\"example\">Example 3:</strong></p>\n\n<pre>\n<strong>Input:</strong> s = &quot;MCMXCIV&quot;\n<strong>Output:</strong> 1994\n<strong>Explanation:</strong> M = 1000, CM = 900, XC = 90 and IV = 4.\n</pre>\n\n<p>&nbsp;</p>\n<p><strong>Constraints:</strong></p>\n\n<ul>\n\t<li><code>1 &lt;= s.length &lt;= 15</code></li>\n\t<li><code>s</code> contains only&nbsp;the characters <code>(&#39;I&#39;, &#39;V&#39;, &#39;X&#39;, &#39;L&#39;, &#39;C&#39;, &#39;D&#39;, &#39;M&#39;)</code>.</li>\n\t<li>It is <strong>guaranteed</strong>&nbsp;that <code>s</code> is a valid roman numeral in the range <code>[1, 3999]</code>.</li>\n</ul>\n","clarifyingQuestions":["What is the guaranteed range of the input Roman numeral string 's' (e.g., minimum and maximum length, and if it's guaranteed to be a valid Roman numeral)?","Are there any specific symbols or combinations of symbols that are considered invalid or outside the scope of standard Roman numeral representation that I should handle?","Is it possible for the input string to be empty or null, and if so, what should be the expected output in those cases?","What is the maximum integer value that a valid Roman numeral string can represent? This will help me consider potential integer overflow issues, although less likely with standard Roman numerals.","Are we dealing with standard Roman numeral rules (e.g., 'IIII' is not allowed, only 'IV' for 4), or are there any variations in the rules to consider?"],"edgeCases":[{"edgeCase":"Null or empty input string","handling":"The solution should return 0 for a null or empty input string, as there are no Roman numerals to convert."},{"edgeCase":"Single character Roman numeral (e.g., 'I', 'V')","handling":"The solution should correctly map single characters to their corresponding integer values."},{"edgeCase":"Roman numerals with subtractive notation (e.g., 'IV', 'IX', 'XL', 'XC', 'CD', 'CM')","handling":"The algorithm must be designed to handle these specific combinations by checking preceding characters."},{"edgeCase":"Roman numerals with repeated characters (e.g., 'III', 'XX')","handling":"The solution should correctly sum the values of repeated characters."},{"edgeCase":"Longest possible valid Roman numeral input","handling":"The solution should scale efficiently for longer strings, ensuring performance does not degrade significantly."},{"edgeCase":"Invalid Roman numeral characters (e.g., 'A', 'Z', numbers)","handling":"The problem statement implies valid Roman numeral input; however, a robust solution might throw an error or return a specific indicator for invalid characters."},{"edgeCase":"Roman numerals exceeding the standard representation (e.g., 'MMMM')","handling":"The standard Roman numeral system typically stops at 3999, so inputs beyond that might be considered out of scope or require specific handling if the problem allows for extensions."},{"edgeCase":"All identical values in a sequence that implies subtraction (e.g., 'IIII' instead of 'IV')","handling":"The conversion logic should prioritize subtractive notation over simple summation for valid cases, assuming standard Roman numeral rules."}],"companies":[{"slug":"google","name":"Google","logo":"/logos/google.jpg"},{"slug":"amazon","name":"Amazon","logo":"/logos/amazon.jpg"},{"slug":"microsoft","name":"Microsoft","logo":"/logos/microsoft.jpg"},{"slug":"meta","name":"Meta","logo":"/logos/meta.jpg"},{"slug":"yahoo","name":"Yahoo","logo":"/logos/yahoo.jpg"},{"slug":"bloomberg-lp","name":"Bloomberg","logo":"/logos/bloomberg-lp.jpg"},{"slug":"doordash","name":"DoorDash","logo":"/logos/doordash.png"},{"slug":"uber","name":"Uber","logo":"/logos/uber.jpg"},{"slug":"apple","name":"Apple","logo":"/logos/apple.jpg"},{"slug":"adobe","name":"Adobe","logo":"/logos/adobe.jpg"},{"slug":"ibm","name":"IBM","logo":"/logos/ibm.jpg"},{"slug":"ebay","name":"eBay","logo":"/logos/ebay.jpg"},{"slug":"walmart","name":"Walmart","logo":"/logos/walmart.jpg"},{"slug":"sofi","name":"SoFi","logo":"/logos/sofi.jpg"},{"slug":"tiktok","name":"TikTok","logo":"/logos/tiktok.png"},{"slug":"salesforce","name":"Salesforce","logo":"/logos/salesforce.png"},{"slug":"amd","name":"AMD","logo":"/logos/amd.jpg"},{"slug":"expedia-group","name":"Expedia Group","logo":"/logos/expedia-group.jpg"},{"slug":"snowflake","name":"Snowflake","logo":"/logos/snowflake.jpg"},{"slug":"linkedin","name":"LinkedIn","logo":"/logos/linkedin.png"},{"slug":"goldman-sachs","name":"Goldman Sachs","logo":"/logos/goldman-sachs.jpg"},{"slug":"visa","name":"Visa","logo":"/logos/visa.png"},{"slug":"capital-one","name":"Capital One","logo":"/logos/capital-one.jpg"},{"slug":"bookingcom","name":"Bookingcom","logo":"/logos/bookingcom.jpg"}],"solutions":{"bruteForce":{"overview":"To convert a Roman numeral to an integer using a brute force approach, we can think of it as systematically processing the Roman numeral string from left to right, character by character, and accumulating the corresponding integer value.","steps":["Start with a total value of zero.","Look at the first character of the Roman numeral.","If it's an 'I', add one to the total.","If it's a 'V', add five to the total.","If it's an 'X', add ten to the total.","If it's an 'L', add fifty to the total.","If it's a 'C', add one hundred to the total.","If it's a 'D', add five hundred to the total.","If it's an 'M', add one thousand to the total.","Now, consider the next character. If this character represents a larger value than the previous one, it means we have a subtractive case (like 'IV' meaning four). In this situation, instead of adding the value of the current character, we actually need to subtract the value of the previous character twice and then add the value of the current character.","For example, if we just added one for 'I' and the next character is 'V', we would undo the addition of one (making the total zero again) and then add five, resulting in a total of four.","Continue this process for every character in the Roman numeral, always considering the relationship between the current character and the one immediately before it.","After processing all the characters, the final accumulated total will be the integer representation of the Roman numeral."],"bigO":{"runtimeValue":"O(n)","runtimeExplanation":"The algorithm iterates through the Roman numeral string once, character by character. For each character, it performs a constant number of operations: checking its value and comparing it with the previous character's value.  If n is the length of the Roman numeral string, the total number of operations is directly proportional to n. Therefore, the time complexity is O(n).","spaceValue":"O(1)","spaceExplanation":"The described brute-force approach uses a constant amount of extra space.  A single variable is used to store the 'total value', and another variable (implicitly) to keep track of the 'previous character's value' for subtractive cases. The space used does not depend on the length of the Roman numeral string (N) and remains fixed regardless of the input size."},"languages":["Python","JavaScript","TypeScript","Java","C++","C#","Go","Rust","Kotlin","Swift","Ruby","PHP"],"code":{"Python":"def roman_to_integer_brute_force(roman_numeral_string):    total_integer_value = 0    roman_map = {        'I': 1,        'V': 5,        'X': 10,        'L': 50,        'C': 100,        'D': 500,        'M': 1000    }    for current_index in range(len(roman_numeral_string)):        current_roman_value = roman_map[roman_numeral_string[current_index]]        # If this is not the last character, check if the next character has a larger value        if current_index < len(roman_numeral_string) - 1:            next_roman_value = roman_map[roman_numeral_string[current_index + 1]]            # If the next value is greater, we have a subtractive case            if next_roman_value > current_roman_value:                # We need to subtract the current value twice and then add the next value                # This effectively means we are adding the difference (next_roman_value - current_roman_value)                total_integer_value -= current_roman_value                # Add the current character's value to the total        total_integer_value += current_roman_value    return total_integer_value","JavaScript":"function romanToIntegerBruteForce(romanNumeralString) {\n    let totalValue = 0;\n    const romanValueMap = {\n        'I': 1,\n        'V': 5,\n        'X': 10,\n        'L': 50,\n        'C': 100,\n        'D': 500,\n        'M': 1000\n    };\n\n    for (let currentIndex = 0; currentIndex < romanNumeralString.length; currentIndex++) {\n        const currentCharacter = romanNumeralString[currentIndex];\n        const currentValue = romanValueMap[currentCharacter];\n\n        // Check if there is a next character to compare with\n        if (currentIndex < romanNumeralString.length - 1) {\n            const nextCharacter = romanNumeralString[currentIndex + 1];\n            const nextValue = romanValueMap[nextCharacter];\n\n            // If the current value is less than the next value, it's a subtractive case\n            if (currentValue < nextValue) {\n                // To handle subtractive cases like 'IV' (4) or 'IX' (9),\n                // we subtract the current value twice from the total before adding the next value.\n                // This effectively undoes the addition of the current value and then adds the correct difference.\n                totalValue -= currentValue;\n            } else {\n                // Otherwise, it's an additive case, so we simply add the current value.\n                totalValue += currentValue;\n            }\n        } else {\n            // If it's the last character, we always add its value.\n            totalValue += currentValue;\n        }\n    }\n\n    return totalValue;\n}","TypeScript":"function romanToIntBruteForce(romanNumeralString: string): number {\n    let totalValue: number = 0;\n    const romanValueMap: { [key: string]: number } = {\n        'I': 1,\n        'V': 5,\n        'X': 10,\n        'L': 50,\n        'C': 100,\n        'D': 500,\n        'M': 1000\n    };\n\n    for (let currentIndex = 0; currentIndex < romanNumeralString.length; currentIndex++) {\n        const currentValue: number = romanValueMap[romanNumeralString[currentIndex]];\n        const previousValue: number = (currentIndex > 0) ? romanValueMap[romanNumeralString[currentIndex - 1]] : 0;\n\n        // If the current Roman numeral's value is greater than the previous one, it indicates a subtractive case.\n        if (currentValue > previousValue) {\n            // To correct for the previous addition, subtract it twice and then add the current value.\n            totalValue = totalValue - (2 * previousValue) + currentValue;\n        } else {\n            // Otherwise, simply add the current Roman numeral's value to the total.\n            totalValue += currentValue;\n        }\n    }\n\n    return totalValue;\n}","Java":"class Solution {n    public int romanToInt(String s) {n        int totalValue = 0;n        int previousCharacterValue = 0;n        for (int index = 0; index < s.length(); index++) {n            int currentValue = 0;n            char currentRomanCharacter = s.charAt(index);n            if (currentRomanCharacter == 'I') {n                currentValue = 1;n            } else if (currentRomanCharacter == 'V') {n                currentValue = 5;n            } else if (currentRomanCharacter == 'X') {n                currentValue = 10;n            } else if (currentRomanCharacter == 'L') {n                currentValue = 50;n            } else if (currentRomanCharacter == 'C') {n                currentValue = 100;n            } else if (currentRomanCharacter == 'D') {n                currentValue = 500;n            } else if (currentRomanCharacter == 'M') {n                currentValue = 1000;n            }nn            // If the current character's value is greater than the previous, it's a subtractive case.n            if (currentValue > previousCharacterValue) {n                // We need to undo the addition of the previous character's value and then subtract it again.n                // This effectively subtracts the previous value twice.n                totalValue = totalValue - (previousCharacterValue * 2);n                totalValue = totalValue + currentValue;n            } else {n                // If not a subtractive case, simply add the current character's value.n                totalValue = totalValue + currentValue;n            }n            // Update the previous character's value for the next iteration.n            previousCharacterValue = currentValue;n        }n        return totalValue;n    }n}","C++":"class Solution {public:    int romanToInt(std::string romanNumeralString) {        int totalIntegervalue = 0;        int previousCharacterValue = 0;        for (int stringIndex = 0; stringIndex < romanNumeralString.length(); ++stringIndex) {            int currentCharacterValue = 0;            char currentChar = romanNumeralString[stringIndex];            if (currentChar == 'I') {                currentCharacterValue = 1;            } else if (currentChar == 'V') {                currentCharacterValue = 5;            } else if (currentChar == 'X') {                currentCharacterValue = 10;            } else if (currentChar == 'L') {                currentCharacterValue = 50;            } else if (currentChar == 'C') {                currentCharacterValue = 100;            } else if (currentChar == 'D') {                currentCharacterValue = 500;            } else if (currentChar == 'M') {                currentCharacterValue = 1000;            }            // If the current character's value is greater than the previous one,            // it signifies a subtractive case.            if (currentCharacterValue > previousCharacterValue) {                // Undo the addition of the previous character's value by subtracting it twice,                // then add the current character's value.                totalIntegervalue = totalIntegervalue - 2 * previousCharacterValue + currentCharacterValue;            } else {                // Otherwise, simply add the current character's value to the total.                totalIntegervalue += currentCharacterValue;            }            // Update the previous character's value for the next iteration.            previousCharacterValue = currentCharacterValue;        }        return totalIntegervalue;    }};","C#":"public class RomanToInteger {\n\n    public int RomanToInt(string s) {\n        int totalValue = 0;\n        int previousValue = 0;\n\n        for (int i = 0; i < s.Length; i++) {\n            int currentValue = 0;\n\n            switch (s[i]) {\n                case 'I':\n                    currentValue = 1;\n                    break;\n                case 'V':\n                    currentValue = 5;\n                    break;\n                case 'X':\n                    currentValue = 10;\n                    break;\n                case 'L':\n                    currentValue = 50;\n                    break;\n                case 'C':\n                    currentValue = 100;\n                    break;\n                case 'D':\n                    currentValue = 500;\n                    break;\n                case 'M':\n                    currentValue = 1000;\n                    break;\n            }\n\n            // If the current numeral's value is greater than the previous, we have a subtractive case.\n            if (currentValue > previousValue) {\n                // We need to subtract the previous value twice because it was incorrectly added, then add the current value.\n                totalValue -= 2 * previousValue;\n                // Add the current character's value.\n                totalValue += currentValue;\n            } else {\n                // Otherwise, it's a simple additive case, just add the current character's value.\n                totalValue += currentValue;\n            }\n\n            // Update the previous value to be the current value for the next iteration.\n            previousValue = currentValue;\n        }\n\n        return totalValue;\n    }\n}","Go":"func romanToIntBruteForce(romanNumeralString string) int {n   totalValue := 0n    previousCharacterValue := 0nn    romanValueMap := map[byte]int{n        'I': 1,n        'V': 5,n        'X': 10,n        'L': 50,n        'C': 100,n        'D': 500,n        'M': 1000,n    }nn    for i := 0; i < len(romanNumeralString); i++ {n        currentChar := romanNumeralString[i]n        currentCharacterValue := romanValueMap[currentChar]nn        // If the current character's value is greater than the previous one,n        // we have a subtractive case.        if currentCharacterValue > previousCharacterValue {n            // We need to undo the addition of the previous value and subtract it again,n            // then add the current character's value.            totalValue -= 2 * previousCharacterValuen        }nn        totalValue += currentCharacterValuen        previousCharacterValue = currentCharacterValuen    }nn    return totalValuen}n","Rust":"fn roman_to_int_brute_force(roman_numeral_string: String) -> i32 {    let mut total_integer_value: i32 = 0;    let mut previous_character_value: i32 = 0;    for character in roman_numeral_string.chars() {        let current_character_value = match character {            'I' => 1,            'V' => 5,            'X' => 10,            'L' => 50,            'C' => 100,            'D' => 500,            'M' => 1000,            _ => 0, // Should not happen with valid Roman numerals        };        // If the current numeral's value is greater than the previous one,        // it signifies a subtractive case (e.g., IV, IX).        if current_character_value > previous_character_value {            // We need to subtract the previous value twice to correct the initial addition,            // and then add the current value.            total_integer_value -= 2 * previous_character_value;            total_integer_value += current_character_value;        } else {            // Otherwise, we simply add the current numeral's value to the total.            total_integer_value += current_character_value;        }        previous_character_value = current_character_value;    }    total_integer_value}","Kotlin":"fun romanToIntBruteForce(romanNumeralString: String): Int {    var totalValue = 0    val romanValueMap = mapOf(        'I' to 1,        'V' to 5,        'X' to 10,        'L' to 50,        'C' to 100,        'D' to 500,        'M' to 1000    )    for (currentIndex in romanNumeralString.indices) {        val currentValue = romanValueMap[romanNumeralString[currentIndex]] ?: 0        // We need to check if the current numeral's value is less than the next one.        // This signifies a subtractive case like IV (4) or IX (9).        if (currentIndex < romanNumeralString.length - 1) {            val nextValue = romanValueMap[romanNumeralString[currentIndex + 1]] ?: 0            if (currentValue < nextValue) {                // If it's a subtractive case, we subtract the current value twice                // to effectively undo its previous addition and then prepare to add the next value.                totalValue -= currentValue * 2            }        }        // Add the value of the current Roman numeral to the total.        totalValue += currentValue    }    return totalValue}","Swift":"func romanToIntBruteForce(romanNumeral string) int {    totalValue := 0    previousCharacterValue := 0    romanValueMap := map[rune]int{        'I': 1,        'V': 5,        'X': 10,        'L': 50,        'C': 100,        'D': 500,        'M': 1000,    }    for _, currentCharacter := range romanNumeral {        currentCharacterValue := romanValueMap[currentCharacter]        // If the current character's value is greater than the previous one,        // it signifies a subtractive case.        if currentCharacterValue > previousCharacterValue {            // We need to reverse the addition of the previous character            // before adding the current one to correctly handle subtractions.            totalValue -= 2 * previousCharacterValue        }        // Always add the value of the current character.        totalValue += currentCharacterValue        previousCharacterValue = currentCharacterValue    }    return totalValue}","Ruby":"def roman_to_integer_brute_force(roman_numeral_string)\n  roman_value_map = {\n    'I' => 1,\n    'V' => 5,\n    'X' => 10,\n    'L' => 50,\n    'C' => 100,\n    'D' => 500,\n    'M' => 1000\n  }\n\n  total_integer_value = 0\n  previous_character_value = 0\n\n  roman_numeral_string.each_char do |current_character|\n    current_character_value = roman_value_map[current_character]\n\n    # If the current character's value is greater than the previous, we have a subtractive case\n    if current_character_value > previous_character_value\n      # We need to undo the previous addition and then apply the correct subtraction\n      total_integer_value -= (previous_character_value * 2)\n      total_integer_value += current_character_value\n    else\n      # Otherwise, we simply add the current character's value to the total\n      total_integer_value += current_character_value\n    end\n\n    previous_character_value = current_character_value\n  end\n\n  total_integer_value\nend","PHP":"<?php\n\nfunction romanToIntegerBruteForce(string $romanNumeral): int {\n    $totalValue = 0;\n    $romanMap = [\n        'I' => 1,\n        'V' => 5,\n        'X' => 10,\n        'L' => 50,\n        'C' => 100,\n        'D' => 500,\n        'M' => 1000\n    ];\n\n    $previousCharacterValue = 0;\n\n    for ($characterIndex = 0; $characterIndex < strlen($romanNumeral); $characterIndex++) {\n        $currentRomanCharacter = $romanNumeral[$characterIndex];\n        $currentCharacterValue = $romanMap[$currentRomanCharacter];\n\n        // If the current character's value is greater than the previous one, it indicates a subtractive case.\n        if ($currentCharacterValue > $previousCharacterValue) {\n            // We must undo the addition of the previous value and then subtract it again before adding the current value.\n            $totalValue -= 2 * $previousCharacterValue;\n        }\n\n        // Add the value of the current Roman numeral character to the total.\n        $totalValue += $currentCharacterValue;\n\n        // Store the current character's value for comparison in the next iteration.\n        $previousCharacterValue = $currentCharacterValue;\n    }\n\n    return $totalValue;\n}\n\n?>"}},"optimal":{"overview":"The key is to process the Roman numeral from right to left, which simplifies handling the subtractive rule. By comparing each numeral to the one immediately to its right, we can determine whether to add or subtract its value.","steps":["Imagine you have a little machine that can translate Roman numerals. To make it work efficiently, you'll start reading the numeral from the very end, moving backwards.","As you look at each Roman numeral symbol, compare its value to the value of the symbol that comes right before it (which you've already looked at).","If the current symbol's value is smaller than the one before it, it means we need to subtract this current symbol's value from our running total.","If the current symbol's value is greater than or equal to the one before it, we simply add its value to our running total.","Keep doing this for every symbol, moving from right to left, and the final running total will be the integer representation of the Roman numeral.","This right-to-left approach cleverly handles cases like 'IV' (4) or 'IX' (9) without needing special checks."],"bigO":{"runtimeValue":"O(n)","runtimeExplanation":"The algorithm iterates through the Roman numeral string exactly once, from right to left. For each Roman numeral character, a constant time lookup is performed to get its integer value, and a constant time comparison and addition/subtraction operation is done. Therefore, if 'n' is the length of the Roman numeral string, the total number of operations is directly proportional to 'n', resulting in a time complexity of O(n).","spaceValue":"O(1)","spaceExplanation":"The algorithm primarily uses a single variable to maintain the running total and another variable to store the value of the previously processed Roman numeral.  There are no auxiliary data structures that grow with the input string's length (N).  The memory footprint remains constant regardless of the length of the Roman numeral string."},"languages":["Python","JavaScript","TypeScript","Java","C++","C#","Go","Rust","Kotlin","Swift","Ruby","PHP"],"code":{"Python":"def roman_to_integer(roman_numeral_string):    roman_value_map = {        'I': 1, 'V': 5, 'X': 10, 'L': 50, 'C': 100, 'D': 500, 'M': 1000    }    total_integer_value = 0    previous_symbol_value = 0    # Iterate from right to left to handle subtractive cases easily.    for current_symbol in reversed(roman_numeral_string):        current_symbol_value = roman_value_map[current_symbol]        # If the current symbol's value is less than the previous one, subtract it.        if current_symbol_value < previous_symbol_value:            total_integer_value -= current_symbol_value        else:            # Otherwise, add the current symbol's value to the total.            total_integer_value += current_symbol_value        # Update the previous symbol's value for the next iteration.        previous_symbol_value = current_symbol_value    return total_integer_value","JavaScript":"function romanToInteger(romanNumeralString) {    const romanNumeralValueMap = {        'I': 1,        'V': 5,        'X': 10,        'L': 50,        'C': 100,        'D': 500,        'M': 1000    };    let integerResult = 0;    let previousNumeralValue = 0;    // Process the Roman numeral string from right to left.    for (let index = romanNumeralString.length - 1; index >= 0; index--) {        const currentRomanSymbol = romanNumeralString[index];        const currentNumeralValue = romanNumeralValueMap[currentRomanSymbol];        // If the current numeral's value is less than the previous one, subtract it.        // This handles cases like IV (4) or IX (9) where a smaller numeral precedes a larger one.        if (currentNumeralValue < previousNumeralValue) {            integerResult -= currentNumeralValue;        } else {            // Otherwise, add the current numeral's value to the total.            // This is the standard case for most Roman numerals.            integerResult += currentNumeralValue;        }        // Update the previous numeral value for the next iteration.        // This prepares for the comparison in the next step of the loop.        previousNumeralValue = currentNumeralValue;    }    return integerResult;}","TypeScript":"function romanToInt(romanNumeralString) {n  const romanNumeralValues = {n    'I': 1,n    'V': 5,n    'X': 10,n    'L': 50,n    'C': 100,n    'D': 500,n    'M': 1000n  };n  let integerResult = 0;n  let previousNumeralValue = 0;n  n  // Process the Roman numeral from right to left.  for (let index = romanNumeralString.length - 1; index >= 0; index--) {n    const currentNumeralCharacter = romanNumeralString[index];n    const currentNumeralValue = romanNumeralValues[currentNumeralCharacter];n        // If the current numeral's value is less than the previous one, we subtract.    if (currentNumeralValue < previousNumeralValue) {n      integerResult -= currentNumeralValue;n    } else {    // Otherwise, we add the current numeral's value to the total.      integerResult += currentNumeralValue;    }n    // Update the previous numeral value for the next iteration.    previousNumeralValue = currentNumeralValue;  }n  return integerResult;n}","Java":"class Solution {\n    public int romanToInt(String s) {\n        Map<Character, Integer> romanNumeralValues = new HashMap<>();\n        romanNumeralValues.put('I', 1);\n        romanNumeralValues.put('V', 5);\n        romanNumeralValues.put('X', 10);\n        romanNumeralValues.put('L', 50);\n        romanNumeralValues.put('C', 100);\n        romanNumeralValues.put('D', 500);\n        romanNumeralValues.put('M', 1000);\n\n        int runningTotalValue = 0;\n        int previousNumeralValue = 0;\n\n        // Process the Roman numeral string from right to left.\n        for (int index = s.length() - 1; index >= 0; index--) {\n            char currentCharacter = s.charAt(index);\n            int currentNumeralValue = romanNumeralValues.get(currentCharacter);\n\n            // If the current numeral's value is less than the previous one, subtract it.\n            if (currentNumeralValue < previousNumeralValue) {\n                runningTotalValue -= currentNumeralValue;\n            } else {\n                // Otherwise, add the current numeral's value to the total.\n                runningTotalValue += currentNumeralValue;\n            }\n            // Update the previous numeral value for the next iteration.\n            previousNumeralValue = currentNumeralValue;\n        }\n\n        return runningTotalValue;\n    }\n}","C++":"#include <string>#include <unordered_map>class Solution {public:    int romanToInt(std::string romanNumeralString) {        std::unordered_map<char, int> romanValueMap;        romanValueMap['I'] = 1;        romanValueMap['V'] = 5;        romanValueMap['X'] = 10;        romanValueMap['L'] = 50;        romanValueMap['C'] = 100;        romanValueMap['D'] = 500;        romanValueMap['M'] = 1000;        int totalIntegerSum = 0;        int previousSymbolValue = 0;        // Process the Roman numeral from right to left to simplify subtractive logic.        for (int index = romanNumeralString.length() - 1; index >= 0; --index) {            int currentSymbolValue = romanValueMap[romanNumeralString[index]];            // If the current symbol's value is less than the previous one, subtract it.            if (currentSymbolValue < previousSymbolValue) {                totalIntegerSum -= currentSymbolValue;            }            // Otherwise, add the current symbol's value.            else {                totalIntegerSum += currentSymbolValue;            }            // Update the previous symbol's value for the next iteration.            previousSymbolValue = currentSymbolValue;        }        // The accumulated sum represents the integer equivalent of the Roman numeral.        return totalIntegerSum;    }};","C#":"using System.Collections.Generic;\n\npublic class Solution {\n    public int RomanToInt(string romanNumeralString) {\n        Dictionary<char, int> romanNumeralValues = new Dictionary<char, int>()\n        {\n            {'I', 1},\n            {'V', 5},\n            {'X', 10},\n            {'L', 50},\n            {'C', 100},\n            {'D', 500},\n            {'M', 1000}\n        };\n\n        int integerResult = 0;\n        int previousNumeralValue = 0;\n\n        // Process the Roman numeral from right to left.\n        for (int index = romanNumeralString.Length - 1; index >= 0; index--) {\n            char currentChar = romanNumeralString[index];\n            int currentValue = romanNumeralValues[currentChar];\n\n            // If the current symbol's value is less than the previous one (to its right), subtract it.\n            if (currentValue < previousNumeralValue) {\n                integerResult -= currentValue;\n            } else {\n                // Otherwise, add its value to the running total.\n                integerResult += currentValue;\n            }\n\n            // This ensures we compare against the numeral immediately to the right in the next iteration.\n            previousNumeralValue = currentValue;\n        }\n\n        return integerResult;\n    }\n}","Go":"func romanToInt(romanNumeralString string) int {\n\n\tromanValueMap := map[byte]int{\n\t\t'I': 1,\n\t\t'V': 5,\n\t\t'X': 10,\n\t\t'L': 50,\n\t\t'C': 100,\n\t\t'D': 500,\n\t\t'M': 1000,\n\t}\n\n\tintegerResult := 0\n\tpreviousValue := 0\n\n\t// Process the Roman numeral from right to left for easier subtractive rule handling.\n\tfor i := len(romanNumeralString) - 1; i >= 0; i-- {\n\t\tcurrentValue := romanValueMap[romanNumeralString[i]]\n\n\t\t// If the current numeral's value is less than the previous one, it indicates subtraction.\n\t\tif currentValue < previousValue {\n\t\t\tintegerResult -= currentValue\n\t\t} else {\n\t\t\t// Otherwise, add the current numeral's value to the total.\n\t\t\tintegerResult += currentValue\n\t\t}\n\t\tpreviousValue = currentValue\n\t}\n\n\treturn integerResult\n}","Rust":"use std::collections::HashMap;\n\nfn roman_to_int(roman_numeral_string: String) -> i32 {\n    let mut roman_value_map = HashMap::new();\n    roman_value_map.insert('I', 1);\n    roman_value_map.insert('V', 5);\n    roman_value_map.insert('X', 10);\n    roman_value_map.insert('L', 50);\n    roman_value_map.insert('C', 100);\n    roman_value_map.insert('D', 500);\n    roman_value_map.insert('M', 1000);\n\n    let mut integer_result = 0;\n    let mut previous_symbol_value = 0;\n\n    // Iterate through the Roman numeral string from right to left.\n    for character in roman_numeral_string.chars().rev() {\n        let current_symbol_value = *roman_value_map.get(&character).unwrap();\n\n        // If the current symbol's value is less than the previous one, subtract it.\n        if current_symbol_value < previous_symbol_value {\n            integer_result -= current_symbol_value;\n        } else {\n            // Otherwise, add the current symbol's value to the total.\n            integer_result += current_symbol_value;\n        }\n        // Update the previous symbol's value for the next iteration.\n        previous_symbol_value = current_symbol_value;\n    }\n\n    integer_result\n}\n","Kotlin":"fun romanToInt(romanNumeralString: String): Int {\n    val romanValueMap = mapOf(\n        'I' to 1,\n        'V' to 5,\n        'X' to 10,\n        'L' to 50,\n        'C' to 100,\n        'D' to 500,\n        'M' to 1000\n    )\n\n    var integerResult: Int = 0\n    var previousNumeralValue: Int = 0\n\n    for (index in romanNumeralString.length - 1 downTo 0) {\n        val currentCharacter = romanNumeralString[index]\n        val currentValue = romanValueMap.getValue(currentCharacter)\n\n        // If the current numeral is smaller than the one to its right, subtract its value.\n        if (currentValue < previousNumeralValue) {\n            integerResult -= currentValue\n        } else {\n            // Otherwise, add its value to the total.\n            integerResult += currentValue\n        }\n        \n        // Update the value of the previously processed numeral.\n        previousNumeralValue = currentValue\n    }\n\n    return integerResult\n}","Swift":"func romanToInt(romanNumeralString: String) -> Int {\n    let romanNumeralMap: [Character: Int] = [\n        \"I\": 1,\n        \"V\": 5,\n        \"X\": 10,\n        \"L\": 50,\n        \"C\": 100,\n        \"D\": 500,\n        \"M\": 1000\n    ]\n\n    var totalInteger = 0\n    var previousNumeralValue = 0\n\n    // Process the Roman numeral string from right to left.\n    for currentCharacter in romanNumeralString.reversed() {\n        guard let currentNumeralValue = romanNumeralMap[currentCharacter] else {\n            continue \n        }\n\n        // If the current numeral's value is less than the previous one, subtract it.\n        if currentNumeralValue < previousNumeralValue {\n            totalInteger -= currentNumeralValue\n        } else {\n            // Otherwise, add the current numeral's value.\n            totalInteger += currentNumeralValue\n        }\n        \n        // This updates the value for the next iteration's comparison.\n        previousNumeralValue = currentNumeralValue\n    }\n\n    return totalInteger\n}","Ruby":"def roman_to_integer(roman_numeral_string):\n    roman_value_map = {\n        'I': 1,\n        'V': 5,\n        'X': 10,\n        'L': 50,\n        'C': 100,\n        'D': 500,\n        'M': 1000\n    }\n\n    total_integer_value = 0\n    previous_symbol_value = 0\n\n    # Iterate through the Roman numeral string from right to left.\n    for current_character in reversed(roman_numeral_string):\n        current_symbol_value = roman_value_map[current_character]\n\n        # If the current symbol's value is less than the previous one, subtract it.\n        if current_symbol_value < previous_symbol_value:\n            total_integer_value -= current_symbol_value\n        else:\n            # Otherwise, add the current symbol's value to the total.\n            total_integer_value += current_symbol_value\n\n        # Update the previous symbol's value for the next iteration.\n        previous_symbol_value = current_symbol_value\n\n    return total_integer_value","PHP":"<?php\n\nfunction romanToInteger(string $romanNumeral):\n    int\n{\n    $romanValueMap = [\n        'I' => 1,\n        'V' => 5,\n        'X' => 10,\n        'L' => 50,\n        'C' => 100,\n        'D' => 500,\n        'M' => 1000\n    ];\n\n    $integerResult = 0;\n    $previousSymbolValue = 0;\n\n    $lengthOfNumeral = strlen($romanNumeral);\n\n    // Process the Roman numeral from right to left.\n    for ($index = $lengthOfNumeral - 1; $index >= 0; $index--) {\n        $currentSymbol = $romanNumeral[$index];\n        $currentSymbolValue = $romanValueMap[$currentSymbol];\n\n        // If the current symbol's value is less than the previous one, subtract it.\n        if ($currentSymbolValue < $previousSymbolValue) {\n            $integerResult -= $currentSymbolValue;\n        } else {\n            // Otherwise, add the current symbol's value to the total.\n            $integerResult += $currentSymbolValue;\n        }\n\n        // Update the previous symbol's value for the next iteration.\n        $previousSymbolValue = $currentSymbolValue;\n    }\n\n    return $integerResult;\n}\n\n?>"}}}}